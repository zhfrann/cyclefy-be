openapi: 3.1.0
info:
  title: Cyclefi API Specification
  version: 1.0.0
  description: Dokumentasi API Cyclefi (Bahasa Indonesia)
externalDocs:
  description: Dokumentasi Postman
  url: https://documenter.getpostman.com/view/28800996/2sB3BEmpmo

tags:
  - name: Auth
    description: Dokumentasi seputar autentikasi (registrasi, login, verifikasi email, reset password)
  - name: User
    description: Dokumentasi seputar user (profil, update data)
  - name: Address
    description: Dokumentasi seputar alamat user
  - name: Phone
    description: Dokumentasi seputar nomor kontak user
  - name: Category
    description: Dokumentasi seputar data-data category
  - name: News
    description: Dokumentasi seputar fitur news
  - name: Donation
    description: Dokumentasi seputar fitur donation
  - name: Barter
    description: Dokumentasi seputar fitur barter
  - name: Borrow
    description: Dokumentasi seputar fitur borrow
  - name: Recycle
    description: Dokumentasi seputar fitur recycle
  - name: Repair
    description: Dokumentasi seputar fitur repair
  - name: Notifikasi
    description: Dokumentasi seputar fitur notifikasi
  

paths:
  /api/register:
    post:
      summary: Registrasi User Baru (Manual)
      description: |
        Endpoint untuk registrasi user secara manual. User akan menerima email berisi kode OTP untuk verifikasi email.
        - Jika username atau email sudah terdaftar dan sudah diverifikasi, akan ditolak.
        - Jika email sudah terdaftar tapi belum diverifikasi dan username sama, OTP akan dikirim ulang.
        - Jika username sudah terdaftar, akan ditolak.
      tags: [Auth]
      parameters:
        - name: Accept-Language
          in: header
          required: false
          description: Pengaturan bahasa (en, id) dengan callback ke bahasa Inggris.
          schema:
            type: string
            example: en
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                username:
                  type: string
                  description: Username unik
                  example: johndoe
                email:
                  type: string
                  format: email
                  description: Email user
                  example: john@example.com
                password:
                  type: string
                  format: password
                  description: Password user
                  example: password123
                confirmPassword:
                  type: string
                  format: password
                  description: Konfirmasi password harus sama dengan password
                  example: password123
              required: [username, email, password, confirmPassword]
      responses:
        "201":
          description: Registrasi berhasil, OTP dikirim ke email user
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message:
                    {
                      type: string,
                      example: "Registration successful. Please check your email for OTP.",
                    }
                  data:
                    type: object
                    properties:
                      id: { type: integer, example: 1 }
                      fullname: { type: string, example: "John Doe" }
                      username: { type: string, example: "johndoe" }
                      email: { type: string, example: "john@example.com" }
                      profile_picture:
                        {
                          type: string,
                          example: "http://.../assets/profiles/johndoe.png",
                        }
        "200":
          description: OTP dikirim ulang ke email user yang belum diverifikasi
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message:
                    {
                      type: string,
                      example: "OTP resent, please check your email",
                    }
                  data:
                    type: object
                    properties:
                      email: { type: string, example: "john@example.com" }
        "400":
          description: Validasi gagal atau data sudah terdaftar
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string, example: "Validation failed" }
                  errors:
                    {
                      type: object,
                      example: { email: ["Email sudah terdaftar"] },
                    }

  /api/verify-email:
    post:
      summary: Verifikasi Email User dengan OTP
      description: |
        Endpoint untuk verifikasi email user setelah registrasi menggunakan kode OTP yang dikirim ke email.
      tags: [Auth]
      parameters:
        - name: Accept-Language
          in: header
          required: false
          description: Pengaturan bahasa (en, id) dengan callback ke bahasa Inggris.
          schema:
            type: string
            example: en
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                email:
                  type: string
                  format: email
                  description: Email user
                  example: john@example.com
                otp:
                  type: string
                  description: Kode OTP yang dikirim ke email
                  example: "1234"
              required: [email, otp]
      responses:
        "200":
          description: Email berhasil diverifikasi
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message:
                    { type: string, example: "Email verification successful." }
                  data:
                    type: object
                    properties:
                      email: { type: string, example: "john@example.com" }
                      verified: { type: boolean, example: true }
        "400":
          description: Kode OTP salah atau sudah expired
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string, example: "Invalid or expired OTP" }
                  errors:
                    {
                      type: object,
                      example: { otp: ["OTP salah atau sudah expired"] },
                    }
        "404":
          description: User tidak ditemukan
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string, example: "User not found" }

  /api/resend-email-verification-otp:
    post:
      summary: Kirim Ulang OTP Verifikasi Email
      description: |
        Endpoint untuk mengirim ulang OTP ke email user yang belum diverifikasi.
        - Jika email belum terdaftar, akan ditolak.
        - Jika email sudah terverifikasi, akan ditolak.
        - Jika email belum terverifikasi, OTP baru akan dikirim ke email.
      tags: [Auth]
      parameters:
        - name: Accept-Language
          in: header
          required: false
          description: Pengaturan bahasa (en, id) dengan callback ke bahasa Inggris.
          schema:
            type: string
            example: en
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                email:
                  type: string
                  format: email
                  description: Email user
                  example: john@example.com
              required: [email]
      responses:
        "200":
          description: OTP berhasil dikirim ulang ke email user
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message:
                    {
                      type: string,
                      example: "Resend email verification successfull.",
                    }
                  data:
                    type: object
                    properties:
                      email: { type: string, example: "john@example.com" }
        "400":
          description: Email sudah terverifikasi atau validasi gagal
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message:
                    { type: string, example: "Email sudah terverifikasi" }
                  errors:
                    {
                      type: object,
                      example: { email: ["Email sudah terverifikasi"] },
                    }
        "404":
          description: Email tidak ditemukan
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string, example: "Email not found" }

  /api/send-reset-password-otp:
    post:
      summary: Kirim OTP untuk Reset Password
      description: |
        Endpoint untuk mengirim OTP ke email user yang ingin melakukan reset password.
      tags: [Auth]
      parameters:
        - name: Accept-Language
          in: header
          required: false
          description: Pengaturan bahasa (en, id) dengan callback ke bahasa Inggris.
          schema:
            type: string
            example: en
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                email:
                  type: string
                  format: email
                  description: Email user
                  example: john@example.com
              required: [email]
      responses:
        "200":
          description: OTP berhasil dikirim ke email user
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message: { type: string, example: "OTP sent to email" }
        "404":
          description: Email tidak ditemukan
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string, example: "Email not found" }

  /api/verify-reset-password-otp:
    post:
      summary: Verifikasi OTP untuk Reset Password
      description: |
        Endpoint untuk verifikasi OTP untuk reset password
      tags: [Auth]
      parameters:
        - name: Accept-Language
          in: header
          required: false
          description: Pengaturan bahasa (en, id) dengan callback ke bahasa Inggris.
          schema:
            type: string
            example: en
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                email:
                  type: string
                  format: email
                  description: Email user
                  example: john@example.com
                otp:
                  type: string
                  description: Kode OTP yang dikirim ke email
                  example: "1234"
              required: [email, otp]
      responses:
        "200":
          description: Kode OTP Valid
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message:
                    { type: string, example: "Your OTP Code for reset password is valid" }
                  data:
                    type: object
                    properties:
                      id: { type: integer, example: 1}
                      otp: { type: string, example: "1234"}
                      user:
                        type: object
                        properties:
                          id: {type: integer, example: 1}
                          email: {type: string, example: "johndoe@gmail.com"}
        "404":
          description: User atau Kode OTP reset password tidak ditemukan
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean }
                  message: { type: string }
  
  /api/reset-password:
    post:
      summary: Reset Password dengan OTP
      description: |
        Endpoint untuk reset password menggunakan OTP yang dikirim ke email.
      tags: [Auth]
      parameters:
        - name: Accept-Language
          in: header
          required: false
          description: Pengaturan bahasa (en, id) dengan callback ke bahasa Inggris.
          schema:
            type: string
            example: en
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                email:
                  type: string
                  format: email
                  description: Email user
                  example: john@example.com
                otp:
                  type: string
                  description: Kode OTP yang dikirim ke email
                  example: "1234"
                newPassword:
                  type: string
                  format: password
                  description: Password baru
                  example: "newpassword123"
                confirmNewPassword:
                  type: string
                  format: password
                  description: Konfirmasi password baru harus sama dengan password baru
                  example: "newpassword123"
              required: [email, otp, newPassword, confirmNewPassword]
      responses:
        "200":
          description: Password berhasil direset
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message:
                    { type: string, example: "Password reset successful" }
        "400":
          description: OTP salah/expired atau validasi gagal
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string, example: "Invalid or expired OTP" }
                  errors:
                    {
                      type: object,
                      example: { otp: ["OTP salah atau sudah expired"] },
                    }
        "404":
          description: Email tidak ditemukan
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string, example: "Email not found" }

  /api/users/login:
    post:
      summary: Login manual dengan email/username dan password
      description: |
        Endpoint untuk login manual menggunakan email atau username dan password. Jika berhasil, JWT dikembalikan.
      tags: [Auth]
      parameters:
        - name: Accept-Language
          in: header
          required: false
          description: Pengaturan bahasa (en, id) dengan callback ke bahasa Inggris.
          schema:
            type: string
            example: en
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                identifier:
                  type: string
                  description: Email address atau username
                  example: johndoe
                password:
                  type: string
                  format: password
                  description: Password user
                  example: password123
              required: [identifier, password]
      responses:
        "200":
          description: Login berhasil, JWT dikembalikan
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message: { type: string, example: "Login successful." }
                  data:
                    type: object
                    properties:
                      id: { type: integer, example: 1 }
                      fullname: { type: string, example: "John Doe" }
                      username: { type: string, example: "johndoe" }
                      email: { type: string, example: "john@example.com" }
                      profile_picture:
                        {
                          type: string,
                          example: "http://.../assets/profiles/johndoe.png",
                        }
                  token:
                    {
                      type: string,
                      example: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
                    }
        "401":
          description: Kredensial salah
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string, example: "Username or password wrong" }
        "403":
          description: Akun tidak aktif atau email belum diverifikasi
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string, example: "Account is inactive" }

  /api/auth/google:
    get:
      summary: Redirect ke Google OAuth
      description: |
        Endpoint untuk memulai proses login/register dengan Google OAuth. User akan diarahkan ke halaman login Google.
      tags: [Auth]
      responses:
        "302":
          description: Redirect ke halaman login Google

  /api/auth/google/callback:
    get:
      summary: Callback dari Google OAuth
      description: |
        Endpoint callback setelah user login dengan Google. Jika sukses, user akan otomatis login/register dan menerima JWT.
        - Jika email sudah ada dan belum verifikasi, akun akan diaktifkan.
        - Jika email sudah ada dan sudah verifikasi, user langsung login.
        - Jika email belum ada, user baru akan dibuat.
      tags: [Auth]
      responses:
        "201":
          description: Register/login dengan Google berhasil, JWT dikembalikan
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message:
                    {
                      type: string,
                      example: "Authentication with Google successful",
                    }
                  data:
                    type: object
                    properties:
                      id: { type: integer, example: 1 }
                      fullname: { type: string, example: "John Doe" }
                      username: { type: string, example: "johndoe" }
                      email: { type: string, example: "john@example.com" }
                      profile_picture:
                        {
                          type: string,
                          example: "https://...",
                        }
                  token:
                    {
                      type: string,
                      example: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
                    }
        "400":
          description: Validasi gagal atau error lain
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message:
                    { type: string, example: "Google authentication failed" }

  /api/auth/facebook:
    get:
      summary: Redirect ke Facebook OAuth
      description: |
        Endpoint untuk memulai proses login/register dengan Facebook OAuth. User akan diarahkan ke halaman login Facebook.
      tags: [Auth]
      responses:
        "302":
          description: Redirect ke halaman login Facebook

  /api/auth/facebook/callback:
    get:
      summary: Callback dari Facebook OAuth
      description: |
        Endpoint callback setelah user login dengan Facebook. Jika sukses, user akan otomatis login/register dan menerima JWT.
        - Jika email sudah ada dan belum verifikasi, akun akan diaktifkan.
        - Jika email sudah ada dan sudah verifikasi, user langsung login.
        - Jika email belum ada, user baru akan dibuat.
      tags: [Auth]
      responses:
        "201":
          description: Register/login dengan Facebook berhasil, JWT dikembalikan
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message:
                    {
                      type: string,
                      example: "Authentication with Facebook successful",
                    }
                  data:
                    type: object
                    properties:
                      id: { type: integer, example: 1 }
                      fullname: { type: string, example: "John Doe" }
                      username: { type: string, example: "johndoe" }
                      email: { type: string, example: "john@example.com" }
                      profile_picture:
                        {
                          type: string,
                          example: "https://",
                        }
                  token:
                    {
                      type: string,
                      example: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
                    }
        "400":
          description: Validasi gagal atau error lain
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message:
                    { type: string, example: "Facebook authentication failed" }

  /api/auth/twitter:
    get:
      summary: Redirect ke Twitter OAuth
      description: |
        Endpoint untuk memulai proses login/register dengan Twitter OAuth. User akan diarahkan ke halaman login Twitter.
      tags: [Auth]
      responses:
        "302":
          description: Redirect ke halaman login Twitter

  /api/auth/twitter/callback:
    get:
      summary: Callback dari Twitter OAuth
      description: |
        Endpoint callback setelah user login dengan Twitter. Jika sukses, user akan otomatis login/register dan menerima JWT.
        - Jika email sudah ada dan belum verifikasi, akun akan diaktifkan.
        - Jika email sudah ada dan sudah verifikasi, user langsung login.
        - Jika email belum ada, user baru akan dibuat.
      tags: [Auth]
      responses:
        "201":
          description: Register/login dengan Twitter berhasil, JWT dikembalikan
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message:
                    {
                      type: string,
                      example: "Authentication with Twitter successful",
                    }
                  data:
                    type: object
                    properties:
                      id: { type: integer, example: 1 }
                      fullname: { type: string, example: "John Doe" }
                      username: { type: string, example: "johndoe" }
                      email: { type: string, example: "john@example.com" }
                      profile_picture:
                        {
                          type: string,
                          example: "https://...",
                        }
                  token:
                    {
                      type: string,
                      example: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
                    }
        "400":
          description: Validasi gagal atau error lain
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message:
                    { type: string, example: "Twitter authentication failed" }

  /api/users/current:
    get:
      summary: Mendapatkan data user saat ini
      description: |
        Mengambil data profil user yang sedang login, termasuk relasi phones dan addresses.
      tags: [User]
      parameters:
        - name: Authorization
          in: header
          required: true
          description: Bearer token JWT
          schema:
            type: string
            example: "Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
        - name: Accept-Language
          in: header
          required: false
          description: Pengaturan bahasa (en, id) dengan callback ke bahasa Inggris.
          schema:
            type: string
            example: en
      responses:
        "200":
          description: Get current user successful
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message:
                    { type: string, example: "Get current user successful" }
                  data:
                    type: object
                    properties:
                      id: { type: integer, example: 1 }
                      fullname: { type: string, example: "John Doe" }
                      username: { type: string, example: "johndoe" }
                      email: { type: string, example: "john@example.com" }
                      is_email_verified: { type: boolean, example: true }
                      email_verified_at:
                        {
                          type: string,
                          format: date-time,
                          example: "2025-08-12T10:00:00Z",
                        }
                      profile_picture:
                        {
                          type: string,
                          example: "http://.../assets/profiles/johndoe.png",
                        }
                      is_active: { type: boolean, example: true }
                      phones:
                        type: array
                        items:
                          type: object
                          properties:
                            id: { type: integer, example: 1 }
                            number: { type: string, example: "08123456789" }
                      addresses:
                        type: array
                        items:
                          type: object
                          properties:
                            id: { type: integer, example: 1 }
                            address_name: { type: string, example: "Rumah" }
                            address:
                              { type: string, example: "Jl. Mawar No. 1" }
                            latitude: { type: number, example: -6.9 }
                            longitude: { type: number, example: 107.6 }
                            city: {type: string, example: "Unaaha"}
                            state: {type: string, example: "Sulawesi Tenggara"}
                            country: {type: string, example: "Indonesia"}
                            country_code: {type: string, example: "ID"}
                            zipcode: {type: string, example: "93418"}
                            created_at: {type: string, example: "2025-08-14T00:50:59.220Z"}
                            updated_at: {type: string, example: "2025-08-14T00:50:59.220Z"}
        "401":
          description: Unauthorized (token tidak valid/expired)
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  message:
                    type: string
                    example: "Unauthorized. Need Authorization Token"
                  errors:
                    type: object
                    properties:
                      token:
                        type: string
                        example: null

    patch:
      summary: Update data profil user saat ini
      description: |
        Mengupdate data profil user (fullname, username, password). Hanya field yang dikirim yang diupdate.
        Untuk update password, wajib mengirim oldPassword, password baru, dan confirmPassword.
      tags: [User]
      parameters:
        - name: Authorization
          in: header
          required: true
          description: Bearer token JWT
          schema:
            type: string
            example: "Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
        - name: Accept-Language
          in: header
          required: false
          description: Pengaturan bahasa (en, id) dengan callback ke bahasa Inggris.
          schema:
            type: string
            example: en
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                fullname: { type: string, example: "John Doe" }
                username: { type: string, example: "johndoe" }
                oldPassword:
                  { type: string, format: password, example: "oldpassword123" }
                password:
                  { type: string, format: password, example: "newpassword123" }
                confirmPassword:
                  { type: string, format: password, example: "newpassword123" }
              description: Kirim hanya field yang ingin diupdate. Untuk update password, wajib oldPassword, password, confirmPassword.
      responses:
        "200":
          description: Update current user successful
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message:
                    { type: string, example: "Update current user successful" }
                  data:
                    type: object
                    example:
                      fullname: "John Doe"
                      username: "johndoe"
        "400":
          description: Validasi gagal atau old password salah
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string, example: "Validation failed" }
                  errors:
                    {
                      type: object,
                      example: { oldPassword: ["Old password is incorrect"] },
                    }
        "401":
          description: Unauthorized (token tidak valid/expired)
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  message:
                    type: string
                    example: "Unauthorized. Need Authorization Token"
                  errors:
                    type: object
                    properties:
                      token:
                        type: string
                        example: null

  /api/users/current/profile-picture:
    patch:
      summary: Upload foto profil user saat ini
      description: |
        Mengganti foto profil user yang sedang login. File gambar dikirim sebagai form-data dengan field profile_picture
      tags:
        - User
      parameters:
        - name: Authorization
          in: header
          required: true
          description: Bearer token JWT
          schema:
            type: string
            example: "Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
        - name: Accept-Language
          in: header
          required: false
          description: Pengaturan bahasa (en, id) dengan callback ke bahasa Inggris.
          schema:
            type: string
            example: en
        - name: Content-Type
          in: header
          required: true
          schema:
            type: string
            example: multipart/form-data
          description: Tipe konten yang dikirim dalam format multipart/form-data karena ada file yang disertakan
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                profile_picture:
                  type: string
                  format: binary
      responses:
        "200":
          description: Foto profil berhasil diupdate
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: "Update profile picture successful"
                  data:
                    type: object
                    properties:
                      profile_picture:
                        type: string
                        example: "http://.../assets/profiles/user_1_1755334590343.jpg"
        "400":
          description: Validasi gagal atau file tidak ditemukan
          content:
            application/json:
              example:
                success: false
                message: "No file uploaded"
                errors: null
        "401":
          description: Unauthorized (token tidak valid/expired)
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  message:
                    type: string
                    example: "Unauthorized. Need Authorization Token"
                  errors:
                    type: object
                    properties:
                      token:
                        type: string
                        example: null

  /api/users/current/addresses:
    get:
      summary: Mendapatkan daftar alamat user saat ini
      description: |
        Mengambil data alamat user yang sedang login
      tags:
        - Address
      parameters:
        - name: Authorization
          in: header
          required: true
          description: Bearer token JWT
          schema:
            type: string
            example: "Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
        - name: Accept-Language
          in: header
          required: false
          description: Pengaturan bahasa (en, id) dengan callback ke bahasa Inggris.
          schema:
            type: string
            example: en
        - name: Content-Type
          in: header
          required: true
          schema:
            type: string
            example: application/json
          description: Tipe konten yang dikirim dalam format application/json
      responses:
        "200":
          description: "Berhasil mendapatkan daftar alamat pengguna saat ini"
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: "Berhasil mendapatkan daftar alamat pengguna saat ini"
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: integer
                          example: 1
                        address_name:
                          type: string
                          example: "Rumah Utama"
                        address:
                          type: string
                          example: "Jl. Diponegoro No. 1, Bandung, Jawa Barat, Indonesia"
                        latitude:
                          type: number
                          example: -6.90389
                        longitude:
                          type: number
                          example: 107.61861
                        city:
                          type: string
                          example: "Bandung"
                        state:
                          type: string
                          example: "Jawa Barat"
                        country:
                          type: string
                          example: "Indonesia"
                        country_code:
                          type: string
                          example: "ID"
                        zipcode:
                          type: string
                          example: "40115"
        "401":
          description: Unauthorized (token tidak valid/expired)
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  message:
                    type: string
                    example: "Unauthorized. Need Authorization Token"
                  errors:
                    type: object
                    properties:
                      token:
                        type: string
                        example: null
        "404":
          description: Address tidak ditemukan
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string, example: "Address not found" }

    post:
      summary: Tambah alamat baru untuk user saat ini
      description: |
        Endpoint untuk menambah alamat baru milik user yang sedang login. 
        Sistem akan otomatis melakukan geocoding untuk mendapatkan latitude, longitude, dan detail lokasi dari field `address` yang diberikan.
      tags:
        - Address
      parameters:
        - name: Authorization
          in: header
          required: true
          schema:
            type: string
          description: JWT token (Bearer)
        - name: Accept-Language
          in: header
          required: false
          schema:
            type: string
          description: Bahasa response (en/id)
        - name: Content-Type
          in: header
          required: true
          schema:
            type: string
            example: application/json
          description: Tipe konten yang dikirim dalam format application/json
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                addressName:
                  type: string
                  maxLength: 255
                  example: "Rumah Utama"
                address:
                  type: string
                  maxLength: 255
                  example: "Jl. Diponegoro No. 1, Bandung"
              required:
                - address
      responses:
        "201":
          description: Alamat berhasil dibuat
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: "Create address successful."
                  data:
                    type: object
                    properties:
                      id:
                        type: integer
                        example: 1
                      address_name:
                        type: string
                        example: "Rumah Utama"
                      address:
                        type: string
                        example: "Jl. Diponegoro No. 1, Bandung, Jawa Barat, Indonesia"
                      latitude:
                        type: number
                        example: -6.90389
                      longitude:
                        type: number
                        example: 107.61861
                      city:
                        type: string
                        example: "Bandung"
                      state:
                        type: string
                        example: "Jawa Barat"
                      country:
                        type: string
                        example: "Indonesia"
                      country_code:
                        type: string
                        example: "ID"
                      zipcode:
                        type: string
                        example: "40115"
        "400":
          description: Validasi gagal atau geocoding gagal
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  message:
                    type: string
                    example: "Validation failed"
                  errors:
                    type: object
                    example: { "address": ["Field address is required"] }
        "401":
          description: Unauthorized (token tidak valid/expired)
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  message:
                    type: string
                    example: "Unauthorized. Need Authorization Token"
                  errors:
                    type: object
                    properties:
                      token:
                        type: string
                        example: null

  /api/users/current/addresses/{addressId}:
    get:
      summary: Mendapatkan data alamat user saat ini berdasarkan address Id
      description: |
        Mengambil data alamat user yang sedang login
      tags:
        - Address
      parameters:
        - name: Authorization
          in: header
          required: true
          description: Bearer token JWT
          schema:
            type: string
            example: "Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
        - name: Accept-Language
          in: header
          required: false
          description: Pengaturan bahasa (en, id) dengan callback ke bahasa Inggris.
          schema:
            type: string
            example: en
        - name: Content-Type
          in: header
          required: true
          schema:
            type: string
            example: application/json
          description: Tipe konten yang dikirim dalam format application/json
        - name: addressId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: "Berhasil mendapatkan data alamat pengguna saat ini berdasarkan address Id"
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: Berhasil mendapatkan alamat pengguna saat ini berdasarkan ID
                  data:
                    type: object
                    properties:
                      id: { type: integer, example: 1 }
                      address_name: { type: string, example: "Rumah" }
                      address: { type: string, example: "Jl. Mawar No. 1, Unaaha, ..." }
                      latitude: { type: number, example: -6.9 }
                      longitude: { type: number, example: 107.6 }
                      city: {type: string, example: "Unaaha"}
                      state: {type: string, example: "Sulawesi Tenggara"}
                      country: {type: string, example: "Indonesia"}
                      country_code: {type: string, example: "ID"}
                      zipcode: {type: string, example: "93418"}
        "401":
          description: Unauthorized (token tidak valid/expired)
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  message:
                    type: string
                    example: "Unauthorized. Need Authorization Token"
                  errors:
                    type: object
                    properties:
                      token:
                        type: string
                        example: null
        "404":
          description: Address tidak ditemukan
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string, example: "Address not found" }
    
    patch:
      summary: Update alamat user saat ini
      description: |
        Mengupdate alamat milik user yang sedang login.
      tags: [Address]
      parameters:
        - name: Authorization
          in: header
          required: true
          description: Bearer token JWT
          schema:
            type: string
            example: "Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
        - name: Accept-Language
          in: header
          required: false
          description: Pengaturan bahasa (en, id) dengan callback ke bahasa Inggris.
          schema:
            type: string
            example: en
        - name: Content-Type
          in: header
          required: true
          schema:
            type: string
            example: application/json
          description: Tipe konten yang dikirim dalam format application/json
        - name: addressId
          in: path
          required: true
          schema:
            type: integer
          description: ID address yang akan diupdate
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                addressName: { type: string, example: "Rumah" }
                address: { type: string, example: "Jl. Mawar No. 1" }
              description: Kirim field yang ingin diupdate. Latitude dan longitude belum otomatis, akan diupdate menggunakan library geocoding di masa depan.
      responses:
        "200":
          description: Update address successful
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: Berhasil memperbarui alamat pengguna saat ini.
                  data:
                    type: object
                    properties:
                      id: { type: integer, example: 1 }
                      address_name: { type: string, example: "Rumah" }
                      address: { type: string, example: "Jl. Mawar No. 1, Unaaha, ..." }
                      latitude: { type: number, example: -6.9 }
                      longitude: { type: number, example: 107.6 }
                      city: {type: string, example: "Unaaha"}
                      state: {type: string, example: "Sulawesi Tenggara"}
                      country: {type: string, example: "Indonesia"}
                      country_code: {type: string, example: "ID"}
                      zipcode: {type: string, example: "93418"}
        "400":
          description: Validasi gagal
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string, example: "Validation failed" }
                  errors:
                    {
                      type: object,
                      example: { address: ["Address tidak valid"] },
                    }
        "401":
          description: Unauthorized (token tidak valid/expired)
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  message:
                    type: string
                    example: "Unauthorized. Need Authorization Token"
                  errors:
                    type: object
                    properties:
                      token:
                        type: string
                        example: null
        "404":
          description: Address tidak ditemukan
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string, example: "Address not found" }

    delete:
      summary: Menghapus address user saat ini
      description: |
        Menghapus address user yang sedang login saat ini menggunakan request parameter addressId
      tags:
        - Address
      parameters:
        - name: Authorization
          in: header
          required: true
          description: Bearer token JWT
          schema:
            type: string
            example: "Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
        - name: Accept-Language
          in: header
          required: false
          description: Pengaturan bahasa (en, id) dengan callback ke bahasa Inggris.
          schema:
            type: string
            example: en
        - name: Content-Type
          in: header
          required: true
          schema:
            type: string
            example: application/json
          description: Tipe konten yang dikirim dalam format application/json
        - name: addressId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Berhasil menghapus alamat user
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: Berhasil menghapus alamat pengguna berdasarkan ID
                  data: 
                    type: object
                    properties:
                      id: { type: integer, example: 1 }
                      address_name: { type: string, example: "Rumah" }
                      address: { type: string, example: "Jl. Mawar No. 1, Unaaha, ...." }
                      latitude: { type: number, example: -6.9 }
                      longitude: { type: number, example: 107.6 }
                      city: {type: string, example: "Unaaha"}
                      state: {type: string, example: "Sulawesi Tenggara"}
                      country: {type: string, example: "Indonesia"}
                      country_code: {type: string, example: "ID"}
                      zipcode: {type: string, example: "93418"}
        "401":
          description: Unauthorized (token tidak valid/expired)
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  message:
                    type: string
                    example: "Unauthorized. Need Authorization Token"
                  errors:
                    type: object
                    properties:
                      token:
                        type: string
                        example: null
        "404":
          description: Address tidak ditemukan
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string, example: "Address not found" }

  /api/users/current/phones:
    get:
      summary: Mendapatkan daftar nomor kontak user saat ini
      description: |
        Mengambil seluruh daftar nomor kontak (phone) milik user yang sedang login.
        - Hanya dapat diakses oleh user yang sudah login (dengan Authorization token).
      tags: [Phone]
      parameters:
        - name: Authorization
          in: header
          required: true
          description: Bearer token JWT
          schema:
            type: string
            example: "Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
        - name: Accept-Language
          in: header
          required: false
          description: Pengaturan bahasa (en, id) dengan callback ke bahasa Inggris.
          schema:
            type: string
            example: en
        - name: Content-Type
          in: header
          required: true
          schema:
            type: string
            example: application/json
          description: Tipe konten yang dikirim dalam format application/json
      responses:
        "200":
          description: Daftar nomor kontak user berhasil diambil
          content:
            application/json:
              example:
                success: true
                message: Berhasil mendapatkan daftar nomor telepon.
                data:
                  - id: 1
                    number: "08123456789"
                  - id: 2
                    number: "08987654321"
        "401":
          description: Unauthorized (token tidak valid/expired)
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  message:
                    type: string
                    example: "Unauthorized. Need Authorization Token"
                  errors:
                    type: object
                    properties:
                      token:
                        type: string
                        example: null
        "404":
          description: Address tidak ditemukan
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string, example: "Address not found" }

    post:
      summary: Tambah nomor kontak baru untuk user saat ini
      description: |
        Menambahkan nomor kontak (phone) baru untuk user yang sedang login.
        - Nomor telepon tidak boleh duplikat untuk user yang sama.
      tags: [Phone]
      parameters:
        - name: Authorization
          in: header
          required: true
          description: Bearer token JWT
          schema:
            type: string
            example: "Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
        - name: Accept-Language
          in: header
          required: false
          description: Pengaturan bahasa (en, id) dengan callback ke bahasa Inggris.
          schema:
            type: string
            example: en
      requestBody:
        required: true
        content:
          application/json:
            example:
              number: "08123456789"
      responses:
        "201":
          description: Nomor kontak berhasil ditambahkan
          content:
            application/json:
              example:
                success: true
                message: Berhasil membuat nomor telepon.
                data:
                  id: 3
                  number: "08123456789"
                  user_id: 1
        "400":
          description: Validasi gagal atau nomor telepon sudah ada
          content:
            application/json:
              examples:
                validasi:
                  value:
                    success: false
                    message: Validasi gagal
                    errors:
                      number: "Field number wajib diisi"
                duplikat:
                  value:
                    success: false
                    message: Nomor telepon sudah ditambahkan
                    errors: null
        "401":
          description: Unauthorized (token tidak valid/expired)
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  message:
                    type: string
                    example: "Unauthorized. Need Authorization Token"
                  errors:
                    type: object
                    properties:
                      token:
                        type: string
                        example: null

  /api/users/current/phones/{phoneId}:
    get:
      summary: Mendapatkan detail nomor kontak user saat ini berdasarkan phoneId
      description: |
        Mengambil detail nomor kontak (phone) milik user yang sedang login berdasarkan ID.
      tags: [Phone]
      parameters:
        - name: Authorization
          in: header
          required: true
          description: Bearer token JWT
          schema:
            type: string
            example: "Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
        - name: Accept-Language
          in: header
          required: false
          description: Pengaturan bahasa (en, id) dengan callback ke bahasa Inggris.
          schema:
            type: string
            example: en
        - name: Content-Type
          in: header
          required: true
          schema:
            type: string
            example: application/json
          description: Tipe konten yang dikirim dalam format application/json
        - name: phoneId
          in: path
          required: true
          schema:
            type: integer
          description: ID phone yang akan diambil
      responses:
        "200":
          description: Detail nomor kontak ditemukan
          content:
            application/json:
              example:
                success: true
                message: Berhasil mendapatkan detail nomor telepon.
                data:
                  id: 1
                  number: "08123456789"
                  user_id: 1
        "400":
          description: phoneId bukan angka
          content:
            application/json:
              example:
                success: false
                message: phone id bukan angka
                errors: null
        "401":
          description: Unauthorized (token tidak valid/expired)
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  message:
                    type: string
                    example: "Unauthorized. Need Authorization Token"
                  errors:
                    type: object
                    properties:
                      token:
                        type: string
                        example: null
        "404":
          description: Nomor kontak tidak ditemukan
          content:
            application/json:
              example:
                success: false
                message: Nomor telepon tidak ditemukan
                errors: null
    
    patch:
      summary: Update nomor kontak user saat ini
      description: |
        Mengupdate nomor kontak milik user yang sedang login.
      tags: [Phone]
      parameters:
        - name: Authorization
          in: header
          required: true
          description: Bearer token JWT
          schema:
            type: string
            example: "Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
        - name: Accept-Language
          in: header
          required: false
          description: Pengaturan bahasa (en, id) dengan callback ke bahasa Inggris.
          schema:
            type: string
            example: en
        - name: Content-Type
          in: header
          required: true
          schema:
            type: string
            example: application/json
          description: Tipe konten yang dikirim dalam format application/json
        - name: phoneId
          in: path
          required: true
          schema:
            type: integer
          description: ID phone yang akan diupdate
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                number: { type: string, example: "08123456789" }
              description: Kirim field yang ingin diupdate.
      responses:
        "200":
          description: Update phone successful
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message:
                    {
                      type: string,
                      example: "Update phone for current user successful",
                    }
                  data:
                    type: object
                    example:
                      number: "08123456789"
        "400":
          description: Validasi gagal
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string, example: "Validation failed" }
                  errors:
                    { type: object, example: { number: ["Nomor tidak valid"] } }
        "401":
          description: Unauthorized (token tidak valid/expired)
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  message:
                    type: string
                    example: "Unauthorized. Need Authorization Token"
                  errors:
                    type: object
                    properties:
                      token:
                        type: string
                        example: null
        "404":
          description: Phone tidak ditemukan
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string, example: "Phone not found" }
          application/json:
            schema:
              type: object
              properties:
                success:
                  type: boolean
                message:
                  type: string
                data:
                  type: object
                  properties:
                    email:
                      type: string

    delete:
      summary: Menghapus nomor kontak user saat ini
      description: |
        Menghapus nomor kontak (phone) milik user yang sedang login berdasarkan ID.
      tags: [Phone]
      parameters:
        - name: Authorization
          in: header
          required: true
          description: Bearer token JWT
          schema:
            type: string
            example: "Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
        - name: Accept-Language
          in: header
          required: false
          description: Pengaturan bahasa (en, id) dengan callback ke bahasa Inggris.
          schema:
            type: string
            example: en
        - name: Content-Type
          in: header
          required: true
          schema:
            type: string
            example: application/json
          description: Tipe konten yang dikirim dalam format application/json
        - name: phoneId
          in: path
          required: true
          schema:
            type: integer
          description: ID phone yang akan dihapus
      responses:
        "200":
          description: Nomor kontak berhasil dihapus
          content:
            application/json:
              example:
                success: true
                message: Berhasil menghapus nomor telepon.
                data:
                  id: 1
                  number: "08123456789"
                  user_id: 1
        "400":
          description: phoneId bukan angka
          content:
            application/json:
              example:
                success: false
                message: phone id bukan angka
                errors: null
        "401":
          description: Unauthorized (token tidak valid/expired)
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  message:
                    type: string
                    example: "Unauthorized. Need Authorization Token"
                  errors:
                    type: object
                    properties:
                      token:
                        type: string
                        example: null
        "404":
          description: Nomor kontak tidak ditemukan
          content:
            application/json:
              example:
                success: false
                message: Nomor telepon tidak ditemukan
                errors: null

  /api/users/current/notifications:
    get:
      tags:
        - Notifikasi
      summary: Mendapatkan daftar notifikasi user saat ini
      description: |
        Mendapatkan daftar notifikasi milik user yang sedang login.  
        Mendukung filter berdasarkan tipe notifikasi (`type`) dan periode waktu (`period`), serta pagination.
      parameters:
        - in: query
          name: type
          schema:
            type: string
            enum: [all, donation, barter, borrow, recycle, repair]
            default: all
          description: "Filter berdasarkan tipe notifikasi (default: all)"
        - in: query
          name: period
          schema:
            type: string
            enum: [today, yesterday, a week ago, a month ago]
            default: today
          description: "Filter berdasarkan periode waktu notifikasi (default: today)"
        - in: query
          name: page
          schema:
            type: integer
            default: 1
            minimum: 1
          description: "Nomor halaman (default: 1)"
        - in: query
          name: size
          schema:
            type: integer
            default: 10
            minimum: 1
            maximum: 100
          description: "Jumlah notifikasi per halaman (default: 10)"
        - name: Authorization
          in: header
          required: true
          schema:
            type: string
          description: JWT token (Bearer)
        - name: Accept-Language
          in: header
          required: false
          schema:
            type: string
          description: Bahasa response (en/id)
      responses:
        '200':
          description: Daftar notifikasi user
          content:
            application/json:
              example:
                success: true
                meta:
                  total: 3
                  page: 1
                  size: 10
                  totalPages: 1
                notifications:
                  - id: 12
                    type: donation
                    entity_id: 101
                    title: "Donation - Submitted"
                    message: "Baju Layak Pakai telah diajukan untuk didonasikan"
                    is_read: false
                    created_at: "2025-09-17T10:00:00.000Z"
                    redirect_to: "https://..."
                  - id: 11
                    type: barter
                    entity_id: 88
                    title: "Barter - Confirmed"
                    message: "Permintaan barter Anda telah dikonfirmasi"
                    is_read: true
                    created_at: "2025-09-16T09:00:00.000Z"
                    redirect_to: "htts://..."
        '401':
          description: Tidak terautentikasi

  /api/users/current/notifications/{notificationId}/read:
    patch:
      tags:
        - Notifikasi
      summary: Tandai notifikasi sebagai sudah dibaca
      description: "Tandai notifikasi tertentu milik user saat ini sebagai sudah dibaca (`is_read: true`)"
      parameters:
        - in: path
          name: notificationId
          required: true
          schema:
            type: integer
          description: ID notifikasi
        - name: Authorization
          in: header
          required: true
          schema:
            type: string
          description: JWT token (Bearer)
        - name: Accept-Language
          in: header
          required: false
          schema:
            type: string
          description: Bahasa response (en/id)
      responses:
        '200':
          description: Notifikasi berhasil ditandai sebagai sudah dibaca
          content:
            application/json:
              example:
                success: true
                message: "Tandai notifikasi sebagai berhasil dibaca"
                data:
                  count: 1
        '404':
          description: Notifikasi tidak ditemukan
        '401':
          description: Tidak terautentikasi
        '500':
          description: Kesalahan server internal
        
  /api/users/current/notifications/read-all:
    patch:
      tags:
        - Notifikasi
      summary: Tandai semua notifikasi sebagai sudah dibaca
      description: "Tandai seluruh notifikasi milik user saat ini sebagai sudah dibaca (`is_read: true`)"
      responses:
        '200':
          description: Semua notifikasi berhasil ditandai sebagai sudah dibaca
          content:
            application/json:
              example:
                success: true
                message: "Semua notifikasi berhasil ditandai sebagai sudah dibaca"
                data:
                  count: 5
        '401':
          description: Tidak terautentikasi
        '500':
          description: Kesalahan server internal

  /api/categories:
    get:
      summary: Mendapatkan daftar category yang statusnya aktif
      tags:
        - Category
      description: |
        Mengambil semua data category. Dapat dilakukan filter menggunakan query parameter dengan key search yang akan mencari nama kategori yang mengandung value dari search
      parameters:
        - name: Authorization
          in: header
          required: true
          schema:
            type: string
          description: JWT token (Bearer)
        - name: Accept-Language
          in: header
          required: false
          schema:
            type: string
          description: Bahasa response (en/id)
        - name: Content-Type
          in: header
          required: true
          schema:
            type: string
            example: application/json
          description: Tipe konten yang dikirim dalam format application/json
        - name: search
          in: query
          required: false
          schema:
            type: string
          description: Pencarian nama kategori yang mengandung kata search
      responses:
        "200":
          description: Berhasil mendapatkan data category
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: Get list of categories successful
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: integer
                          example: 1
                        name:
                          type: string
                          example: Clothes
                        description:
                          type: string
                          example: Used or new clothes
                        # icon:
                        is_active:
                          type: boolean
                          example: true
        "401":
          description: Unauthorized (token tidak valid/expired)
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  message:
                    type: string
                    example: "Unauthorized. Need Authorization Token"
                  errors:
                    type: object
                    properties:
                      token:
                        type: string
                        example: null
  
  /api/news:
    get:
      tags:
        - News
      summary: Mendapatkan daftar berita
      description: |
        Mendapatkan daftar berita
      parameters:
        - in: query
          name: search
          schema:
            type: string
          description: "cari berdasarkan judul"
        - in: query
          name: orderBy
          schema:
            type: string
            enum: [newest, oldest]
            default: newest
          description: "Urut berdasarkan newest atau oldest (default: newest)"
        - in: query
          name: page
          schema:
            type: integer
            default: 1
            minimum: 1
          description: "Nomor halaman (default: 1)"
        - in: query
          name: size
          schema:
            type: integer
            default: 10
            minimum: 1
            maximum: 100
          description: "Jumlah item per halaman (default: 10)"
        - name: Authorization
          in: header
          required: true
          schema:
            type: string
          description: JWT token (Bearer)
        - name: Accept-Language
          in: header
          required: false
          schema:
            type: string
          description: Bahasa response (en/id)
      responses:
        '200':
          description: List of news
          content:
            application/json:
              example:
                success: true
                meta:
                  total: 25
                  page: 1
                  size: 10
                  totalPages: 3
                data:
                  - id: 1
                    title: "Community Clean Up Event"
                    content: "Join us for a community clean up event this weekend..." # truncated
                    created_at: "2025-09-17T10:00:00.000Z"
                    author:
                      id: 5
                      fullname: "Admin"
                      username: "admin"
                    images:
                      - "https://dummyimage.com/600x400/000/fff&text=news"
                      - "https://dummyimage.com/600x400/000/fff&text=community"
                  - id: 2
                    title: "Donation Drive Success"
                    content: "Thanks to your support, our donation drive was a huge success..." # truncated
                    created_at: "2025-09-16T09:00:00.000Z"
                    author:
                      id: 5
                      fullname: "Admin"
                      username: "admin"
                    images:
                      - "https://dummyimage.com/600x400/000/fff&text=charity"

  /api/news/{newsId}:
    get:
      tags:
        - News
      summary: Mendapatkan detail berita
      description: Mendapatkan detail berita
      parameters:
        - in: path
          name: newsId
          required: true
          schema:
            type: integer
          description: News ID
        - name: Authorization
          in: header
          required: true
          schema:
            type: string
          description: JWT token (Bearer)
        - name: Accept-Language
          in: header
          required: false
          schema:
            type: string
          description: Bahasa response (en/id)
      responses:
        '200':
          description: News detail
          content:
            application/json:
              example:
                success: true
                data:
                  id: 1
                  title: "Community Clean Up Event"
                  content: "Join us for a community clean up event this weekend at the city park. Let's make our environment cleaner together!"
                  created_at: "2025-09-17T10:00:00.000Z"
                  author:
                    id: 5
                    fullname: "Admin"
                    username: "admin"
                  images:
                    - "https://dummyimage.com/600x400/000/fff&text=news"
                    - "https://dummyimage.com/600x400/000/fff&text=community"
        '400':
          description: Id berita bukan angka
        '404':
          description: Berita tidak ditemukan
  
  /api/users/current/donations:
    get:
      summary: Mendapatkan daftar donasi (list, filter, pagination) user saat ini
      description: |
        Mengambil daftar donasi milik user yang sedang login saat ini dengan dukungan filter kategori, status, dan pagination.
        - Hanya data yang dibutuhkan UI yang dikembalikan (item_name, images, category, status, last_update, description).
        - Mendukung filter multi-kategori dan multi-status.
        - Mendukung pagination (page, size).
      tags:
        - Donation
      parameters:
        - name: Authorization
          in: header
          required: true
          schema:
            type: string
          description: JWT token (Bearer)
        - name: Accept-Language
          in: header
          required: false
          schema:
            type: string
          description: Bahasa response (en/id)
        - name: Content-Type
          in: header
          required: true
          schema:
            type: string
            example: application/json
          description: Tipe konten yang dikirim dalam format application/json
        - name: search
          in: query
          required: false
          schema: { type: string }
          description: Pencarian pada item name atau description
        - name: page
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            default: 1
          description: Halaman ke berapa (default 1)
        - name: size
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            default: 10
          description: Jumlah data per halaman (default 10)
        - name: category
          in: query
          required: false
          schema:
            type: string
            example: "books,furniture"
          description: "Filter kategori, bisa lebih dari satu id dipisahkan koma (ex: books,furniture,clothes)"
        - name: status
          in: query
          required: false
          schema:
            type: string
            example: "submitted,confirmed"
          description: "Filter status donasi, bisa lebih dari satu status dipisahkan koma **(Value: submitted,confirmed,completed,failed)**"
      responses:
        "200":
          description: Daftar donasi berhasil diambil
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: Get donation list successful
                  meta:
                    type: object
                    properties:
                      total:
                        type: integer
                        example: 2
                      page:
                        type: integer
                        example: 1
                      size:
                        type: integer
                        example: 10
                      totalPages:
                        type: integer
                        example: 1
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: integer
                          example: 1
                        item_name:
                          type: string
                          example: Baju Layak Pakai
                        description:
                          type: string
                          example: Baju bekas, masih bagus, siap pakai.
                        images:
                          type: array
                          items:
                            type: string
                            format: uri
                          example:
                            - https://images.pexels.com/photos/271168/pexels-photo-271168.jpeg?auto=compress&cs=tinysrgb&h=350
                        category:
                          type: object
                          properties:
                            id:
                              type: integer
                              example: 1
                            name:
                              type: string
                              example: Clothes
                        status:
                          type: object
                          properties:
                            id: 
                              type: integer
                              example: 1
                            status:
                              type: string
                              example: "submitted"
                            updated_at:
                              type: date-time
                              example: "2025-08-25T13:46:42.813Z"
              examples:
                success:
                  value:
                    success: true
                    message: Get donation list successful
                    meta:
                      total: 2
                      page: 1
                      size: 10
                      totalPages: 1
                    data:
                      - id: 1
                        item_name: Baju Layak Pakai
                        description: Baju bekas, masih bagus, siap pakai.
                        images:
                          - https://images.pexels.com/photos/271168/pexels-photo-271168.jpeg?auto=compress&cs=tinysrgb&h=350
                        category:
                          id: 1
                          name: Clothes
                        status:
                          id: 1
                          status: submitted
                          updated_at: "2025-08-25T13:46:42.813Z"
                      - id: 2
                        item_name: Buku Pelajaran
                        description: Buku pelajaran SD-SMP, lengkap dan bersih.
                        images:
                          - https://images.pexels.com/photos/4456815/pexels-photo-4456815.jpeg?auto=compress&cs=tinysrgb&h=350
                          - https://images.pexels.com/photos/271168/pexels-photo-271168.jpeg?auto=compress&cs=tinysrgb&h=350
                          - https://images.pexels.com/photos/1350789/pexels-photo-1350789.jpeg?auto=compress&cs=tinysrgb&h=350
                        category:
                          id: 2
                          name: Books
                        status:
                          id: 1
                          status: submitted
                          updated_at: "2025-08-25T13:46:42.813Z"     
        "401":
            description: Unauthorized (token tidak valid/expired)
            content:
              application/json:
                schema:
                  type: object
                  properties:
                    success:
                      type: boolean
                      example: false
                    message:
                      type: string
                      example: "Unauthorized. Need Authorization Token"
                    errors:
                      type: object
                      properties:
                        token:
                          type: string
                          example: null

  /api/users/current/donations/{donationId}:
    get:
      summary: Mendapatkan detail donasi milik user saat ini
      description: |
        Endpoint untuk mendapatkan detail donasi milik user yang sedang login, termasuk informasi barang, kategori, alamat, gambar, dan seluruh riwayat status donasi.
        Hanya user yang berhak (pemilik donasi) yang dapat mengakses endpoint ini.
      tags:
        - Donation
      parameters:
        - name: donationId
          in: path
          required: true
          schema:
            type: integer
          description: ID donasi yang ingin diambil detailnya
        - name: Authorization
          in: header
          required: true
          schema:
            type: string
          description: JWT token (Bearer)
        - name: Accept-Language
          in: header
          required: false
          schema:
            type: string
          description: Bahasa response (en/id)
        - name: Content-Type
          in: header
          required: true
          schema:
            type: string
            example: application/json
          description: Tipe konten yang dikirim dalam format application/json
      responses:
        "200":
          description: Detail donasi berhasil diambil
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: Detail donasi berhasil diambil
                  data:
                    type: object
                    properties:
                      id:
                        type: integer
                        example: 10
                      item_name:
                        type: string
                        example: Sepeda Bekas
                      description:
                        type: string
                        example: Sepeda bekas, masih layak pakai, warna biru.
                      category:
                        type: object
                        properties:
                          id:
                            type: integer
                            example: 1
                          name:
                            type: string
                            example: Sepeda
                      status:
                        type: string
                        example: "submitted"
                      address:
                        type: object
                        properties:
                          id: { type: integer, example: 2 }
                          name:
                            type: string
                            example: Rumah
                          address: { type: string, example: "Jl. Mawar No. 1 Bandung, Jawa Barat, Indonesia" }
                          latitude: { type: number, example: -6.123 }
                          longitude: { type: number, example: 106.123 }
                      images:
                        type: array
                        items:
                          type: string
                        example:
                          - https://.../donation_1_1692950400000.jpg
                          - https://.../donation_1_1692950401000.jpg
                      status_histories:
                        type: array
                        items:
                          type: object
                          properties:
                            id: { type: integer, example: 1 }
                            status: { type: string, example: submitted }
                            status_detail: { type: string, example: Donasi diajukan }
                            updated_at: { type: string, format: date-time, example: "2025-08-26T10:00:00.000Z" }
        "400":
          description: Parameter donasi id tidak valid
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string, example: "Donasi ID bukan angka" }
                  errors: { type: object, example: null }
        "401":
            description: Unauthorized (token tidak valid/expired)
            content:
              application/json:
                schema:
                  type: object
                  properties:
                    success:
                      type: boolean
                      example: false
                    message:
                      type: string
                      example: "Unauthorized. Need Authorization Token"
                    errors:
                      type: object
                      properties:
                        token:
                          type: string
                          example: null
        "404":
          description: Donasi tidak ditemukan atau bukan milik user
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string, example: "Donasi tidak ditemukan" }
                  errors: { type: object, example: null }

  /api/donations:
    post:
      tags:
        - Donation
      summary: Membuat donasi baru
      description: |
        Endpoint untuk membuat donasi baru. Hanya user yang sudah login yang dapat mengakses endpoint ini.
      parameters:
        - name: Authorization
          in: header
          required: true
          schema:
            type: string
          description: JWT token (Bearer)
        - name: Accept-Language
          in: header
          required: false
          schema:
            type: string
          description: Bahasa response (en/id)
        - name: Content-Type
          in: header
          required: true
          schema:
            type: string
            example: multipart/form-data
          description: Tipe konten yang dikirim dalam format multipart/form-data karena ada file yang disertakan
        - name: category
          in: query
          description: Filter berdasarkan nama kategori (bisa multiple, pisahkan dengan koma, gunakan "all" untuk semua)
          required: false
          schema:
            type: string
            example: elektronik,perabot
        - name: status
          in: query
          description: "Filter status donasi **(value: submitted,confirmed,completed,failed) atau all**"
          required: false
          schema:
            type: string
            example: submitted,confirmed
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                item_name:
                  type: string
                  maxLength: 255
                  example: Sepeda Bekas
                  description: Nama barang yang didonasikan
                description:
                  type: string
                  maxLength: 255
                  example: Sepeda bekas, masih layak pakai, warna biru.
                  description: Deskripsi barang donasi
                category_id:
                  type: integer
                  example: 1
                  description: ID kategori donasi
                address_id:
                  type: integer
                  example: 2
                  description: ID alamat pengambilan barang
                phone_id:
                  type: integer
                  example: 3
                  description: ID nomor kontak yang bisa dihubungi
                images:
                  type: array
                  items:
                    type: string
                    format: binary
                  # description: Daftar file gambar donasi (maksimal 5 gambar)
              required:
                - item_name
                - description
                - category_id
                - address_id
                - phone_id
                - images
      responses:
        '201':
          description: Donasi berhasil dibuat
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: Donasi berhasil dibuat
                  data:
                    type: object
                    properties:
                      id:
                        type: integer
                        example: 10
                      item_name:
                        type: string
                        example: Sepeda Bekas
                      description:
                        type: string
                        example: Sepeda bekas, masih layak pakai, warna biru.
                      category:
                        type: object
                        properties:
                          id:
                            type: integer
                            example: 1
                          name:
                            type: string
                            example: Sepeda
                      address_id:
                        type: integer
                        example: 2
                      phone_id:
                        type: integer
                        example: 3
                      image:
                        type: array
                        items:
                          type: string
                        example:
                          - https://.../donation_1_1692950400000.jpg
                          - https://.../donation_1_1692950401000.jpg
                      status:
                        type: string
                        example: submitted
        '400':
          description: Validasi gagal atau request tidak sesuai
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  message:
                    type: string
                    example: Validasi gagal
                  errors:
                    type: object
                    example:
                      item_name:
                        - Field item_name wajib diisi
        "401":
          description: Unauthorized (token tidak valid/expired)
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  message:
                    type: string
                    example: "Unauthorized. Need Authorization Token"
                  errors:
                    type: object
                    properties:
                      token:
                        type: string
                        example: null

  /api/barters:
    get:
      summary: Mendapatkan daftar postingan barter milik user lain (Discover barter)
      description: |
        Endpoint untuk menampilkan daftar postingan barter milik user lain (bukan milik user yang sedang login).
        Mendukung pencarian, filter kategori, filter jarak maksimum, dan sorting.
      tags:
        - Barter
      parameters:
        - name: Authorization
          in: header
          required: true
          schema:
            type: string
          description: JWT token (Bearer)
        - name: Accept-Language
          in: header
          required: false
          schema:
            type: string
          description: Bahasa response (en/id)
        - name: search
          in: query
          required: false
          schema: { type: string }
          description: Pencarian pada item name atau description
        - name: category
          in: query
          required: false
          schema: { type: string, example: "books,furniture" }
          description: Filter berdasarkan kategori
        - name: maxDistance
          in: query
          required: false
          schema:
            type: number
            example: 2
          description: Filter postingan dengan jarak maksimum (km) dari address user login. Jika diisi, parameter location diabaikan.
        - name: location
          in: query
          required: false
          schema:
            type: string
            example: "Bandung"
          description: Filter berdasarkan nama lokasi. **Diabaikan jika maxDistance diisi**.
        - name: sortBy
          in: query
          required: false
          schema:
            type: string
            enum: [relevance, nearest, newest]
          description: Urutan hasil (relevance, nearest, newest). Default relevance
        - name: page
          in: query
          required: false
          schema: { type: integer, default: 1 }
        - name: size
          in: query
          required: false
          schema: { type: integer, default: 10 }
      responses:
        "200":
          description: Daftar postingan barter berhasil diambil
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message: { type: string, example: "Get list of barters successful" }
                  meta:
                    type: object
                    properties:
                      total: { type: integer, example: 100 }
                      page: { type: integer, example: 1 }
                      size: { type: integer, example: 10 }
                      totalPages: { type: integer, example: 10 }
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        id: { type: integer, example: 10 }
                        item_name: { type: string, example: Buku Komik Naruto }
                        description: { type: string, example: Komik Naruto volume 1-10, kondisi bagus. }
                        category:
                          type: object
                          properties:
                            id: { type: integer, example: 2 }
                            name: { type: string, example: Buku }
                        address:
                          type: object
                          properties:
                            id: { type: integer, example: 3 }
                            address: { type: string, example: "Jl. Mawar No. 1, Kabupaten Bandung, Indonesia" }
                            latitude: { type: number, example: -6.123 }
                            longitude: { type: number, example: 106.123 }
                        distance: { type: number, example: 2.5, description: "Jarak terdekat (km) dari daftar alamat user" }
                        user:
                          type: object
                          properties:
                            id: { type: integer, example: 5 }
                            profile_picture: { type: string, example: "https://.../username.png" }
                            username: { type: string, example: "budi123" }
                            fullname: { type: string, example: "Budi Santoso" }
                        images:
                          type: array
                          items:
                            type: string
                            format: uri
                          example:
                            - https://...
                            - https://...
        "401":
          description: Unauthorized (token tidak valid/expired)
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  message:
                    type: string
                    example: "Unauthorized. Need Authorization Token"
                  errors:
                    type: object
                    properties:
                      token:
                        type: string
                        example: null

    post:
      summary: Membuat postingan barter baru
      description: |
        Endpoint untuk membuat postingan barter. User mengisi data item, kategori, alamat, kontak, dan upload gambar.
        Status awal barter adalah `waiting_for_request`.
      tags:
        - Barter
      parameters:
        - name: Authorization
          in: header
          required: true
          schema:
            type: string
          description: JWT token (Bearer)
        - name: Accept-Language
          in: header
          required: false
          schema:
            type: string
          description: Bahasa response (en/id)
        - name: Content-Type
          in: header
          required: true
          schema:
            type: string
            example: multipart/form-data
          description: Tipe konten yang dikirim dalam format multipart/form-data karena ada file yang disertakan
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                item_name:
                  type: string
                  maxLength: 255
                  example: Buku Komik Naruto
                  description: Nama barang yang ingin dibarterkan
                description:
                  type: string
                  maxLength: 255
                  example: Komik Naruto volume 1-10, kondisi bagus.
                  description: Deskripsi barang barter
                category_id:
                  type: integer
                  example: 2
                  description: ID kategori barang
                address_id:
                  type: integer
                  example: 3
                  description: ID alamat user
                phone_id:
                  type: integer
                  example: 4
                  description: ID nomor kontak user
                images:
                  type: array
                  items:
                    type: string
                    format: binary
                  # description: Daftar file gambar barter (maksimal 5 gambar)
              required:
                - item_name
                - description
                - category_id
                - address_id
                - phone_id
                - images
      responses:
        "201":
          description: Barter berhasil dibuat
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: Barter berhasil dibuat
                  data:
                    type: object
                    properties:
                      id:
                        type: integer
                        example: 10
                      item_name:
                        type: string
                        example: Buku Komik Naruto
                      description:
                        type: string
                        example: Komik Naruto volume 1-10, kondisi bagus.
                      category:
                        type: object
                        properties:
                          id:
                            type: integer
                            example: 2
                          name:
                            type: string
                            example: Buku
                      address_id:
                        type: integer
                        example: 3
                      phone_id:
                        type: integer
                        example: 4
                      images:
                        type: array
                        items:
                          type: string
                        example:
                          - https://api.cyclefi.id/assets/barters/postings/barter_post_1_1692950400000.jpg
                          - https://api.cyclefi.id/assets/barters/postings/barter_post_1_1692950401000.jpg
                      status:
                        type: string
                        example: Waiting For Request
        "400":
          description: Validasi gagal atau request tidak sesuai
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string, example: "Validasi gagal" }
                  errors:
                    type: object
                    example:
                      item_name:
                        - Field item_name wajib diisi
        "401":
          description: Unauthorized (token tidak valid/expired)
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  message:
                    type: string
                    example: "Unauthorized. Need Authorization Token"
                  errors:
                    type: object
                    properties:
                      token:
                        type: string
                        example: null

  /api/barters/{barterId}:
    get:
      tags:
        - Barter
      summary: Mendapatkan detail postingan barter user lain
      description: |
        Mengambil detail postingan barter milik user lain berdasarkan barterId. Hanya bisa mengakses postingan barter milik user lain, bukan milik sendiri.
      parameters:
        - name: barterId
          in: path
          required: true
          schema:
            type: integer
          description: ID barter yang ingin diambil detailnya
        - name: Authorization
          in: header
          required: true
          schema:
            type: string
          description: JWT token (Bearer)
        - name: Accept-Language
          in: header
          required: false
          schema:
            type: string
          description: Bahasa response (en/id)
        - name: Content-Type
          in: header
          required: true
          schema:
            type: string
            example: application/json
          description: Tipe konten yang dikirim dalam format application/json
      responses:
        '200':
          description: Berhasil mendapatkan detail barter
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: Berhasil mendapatkan detail barter
                  data:
                    type: object
                    properties:
                      id:
                        type: integer
                        example: 1
                      item_name:
                        type: string
                        example: "Tes barter post dewi lestari"
                      description:
                        type: string
                        example: "Tes barter desc"
                      images:
                        type: array
                        items:
                          type: string
                          format: uri
                        example:
                          - "http:/..."
                          - "http:/..."
                      category:
                        type: object
                        nullable: true
                        properties:
                          id:
                            type: integer
                            example: 1
                          name:
                            type: string
                            example: "Clothes"
                      status:
                        type: object
                        nullable: true
                        properties:
                          id:
                            type: integer
                            example: 1
                          status:
                            type: string
                            example: "Waiting For Request"
                          updated_at:
                            type: string
                            format: date-time
                            example: "2025-08-28T10:27:14.759Z"
                      user:
                        type: object
                        nullable: true
                        properties:
                          id:
                            type: integer
                            example: 2
                          profile_picture:
                            type: string
                            format: uri
                            example: "https://..."
                          username:
                            type: string
                            example: "dewi.lestari"
                          fullname:
                            type: string
                            example: "Dewi Lestari"
                      address:
                        type: object
                        nullable: true
                        properties:
                          id:
                            type: integer
                            example: 2
                          address:
                            type: string
                            example: "Jalan Sukapura, Sukapura, Dayeuhkolot, Kabupaten Bandung, Jawa Barat, Jawa, 40257, Indonesia"
                          latitude:
                            type: number
                            example: -6.9697397
                          longitude:
                            type: number
                            example: 107.634133
                      distance:
                        type: number
                        nullable: true
                        example: 0.603
                      phone:
                        type: object
                        nullable: true
                        properties:
                          id:
                            type: integer
                            example: 2
                          number:
                            type: string
                            example: "08123456789"
        '400':
          description: barterId tidak valid
        "401":
          description: Unauthorized (token tidak valid/expired)
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  message:
                    type: string
                    example: "Unauthorized. Need Authorization Token"
                  errors:
                    type: object
                    properties:
                      token:
                        type: string
                        example: null
        '404':
          description: Data barter tidak ditemukan

  /api/barters/{barterId}/request:
    post:
      summary: Ajukan Permintaan Barter ke Postingan Barter User lain
      description: |
        Endpoint untuk mengajukan permintaan barter ke postingan barter milik user lain. User dapat memilih menggunakan item barter yang sudah ada (data dapat diambil dari GET /api/users/current/barters dengan filter ownership=my_items), atau mengisi data item baru beserta upload gambar. 
        - Hanya bisa mengajukan ke postingan barter milik user lain.
        - Tidak boleh mengajukan barter dengan item postingan milik anda yang sama lebih dari satu kali.
        - Jika menggunakan item yang sudah ada, gambar akan otomatis diambil dari item tersebut.
      tags:
        - Barter
      parameters:
        - name: barterId
          in: path
          required: true
          schema:
            type: integer
          description: ID barter yang akan diajukan permintaan barter
        - name: Authorization
          in: header
          required: true
          schema:
            type: string
          description: JWT token (Bearer)
        - name: Accept-Language
          in: header
          required: false
          schema:
            type: string
          description: Bahasa response (en/id)
        - name: Content-Type
          in: header
          required: true
          schema:
            type: string
            example: multipart/form-data
          description: Tipe konten yang dikirim dalam format multipart/form-data karena ada file yang disertakan
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                use_existing_barter_id:
                  type: integer
                  description: ID barter milik sendiri yang ingin digunakan sebagai item barter (opsional, jika ingin pakai item milik user yang sudah ada)
                item_name:
                  type: string
                  maxLength: 255
                  description: Nama item yang ditawarkan **(wajib diisi jika tidak pakai use_existing_barter_id)**
                description:
                  type: string
                  maxLength: 255
                  description: Deskripsi item yang ditawarkan **(wajib diisi jika tidak pakai use_existing_barter_id)**
                category_id:
                  type: integer
                  description: ID kategori item **(wajib diisi jika tidak pakai use_existing_barter_id)**
                address_id:
                  type: integer
                  description: ID alamat user **(wajib diisi jika tidak pakai use_existing_barter_id)**
                phone_id:
                  type: integer
                  description: ID nomor kontak user **(wajib diisi jika tidak pakai use_existing_barter_id)**
                images:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: Upload gambar item barter **(wajib diisi jika tidak pakai use_existing_barter_id)**
      responses:
        "201":
          description: Pengajuan barter berhasil dibuat
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: Pengajuan barter berhasil dibuat
                  data:
                    type: object
                    properties:
                      id:
                        type: integer
                        description: ID barter application
                      barter_id:
                        type: integer
                        description: ID barter yang diajukan
                      item_name:
                        type: string
                        description: Nama item yang ditawarkan
                      description:
                        type: string
                        description: Deskripsi item
                      category_id:
                        type: integer
                        description: ID kategori item
                      address_id:
                        type: integer
                        description: ID alamat user
                      phone_id:
                        type: integer
                        description: ID nomor kontak user
                      images:
                        type: array
                        items:
                          type: string
                          format: uri
                        description: Daftar URL gambar item barter
                      status:
                        type: string
                        example: Request Submitted
                        description: Status aplikasi barter (request_submitted)
        "400":
          description: Request tidak valid (validasi gagal, file tidak diupload, dsb)
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  message:
                    type: string
                    example: Validasi gagal
                  errors:
                    {
                      type: object,
                      example: { item_name: ["Kolom item_name wajib diisi"] },
                    }
        "401":
          description: Unauthorized (token tidak valid/expired)
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  message:
                    type: string
                    example: "Unauthorized. Need Authorization Token"
                  errors:
                    type: object
                    properties:
                      token:
                        type: string
                        example: null
        "403":
          description: |
            - Tidak bisa request to barter pada postingan sendiri
            - Postingan yang diajukan untuk barter berada dalam status yang tidak available
        "404":
          description: Barter tidak ditemukan, barter milik sendiri, atau barter item milik sendiri tidak ditemukan
        "409":
          description: Duplikasi pengajuan barter dengan postingan milik user (sudah pernah mengajukan barter dengan postingan item yang sama)

  /api/users/current/barters:
    get:
      tags:
        - Barter
      summary: Mendapatkan riwayat barter user (posting & request)
      description: |
        Mengambil daftar barter milik user saat ini (my_items) dan request barter yang masuk ke postingan user (other_items), dengan filter kategori, status, search, dan pagination terpisah.
      parameters:
        - name: Authorization
          in: header
          required: true
          schema:
            type: string
          description: JWT token (Bearer)
        - name: Accept-Language
          in: header
          required: false
          schema:
            type: string
          description: Bahasa response (en/id)
        - name: Content-Type
          in: header
          required: true
          schema:
            type: string
            example: application/json
          description: Tipe konten yang dikirim dalam format application/json
        - name: search
          in: query
          description: Pencarian nama item atau deskripsi (optional)
          required: false
          schema:
            type: string
        - name: category
          in: query
          description: Filter berdasarkan nama kategori (bisa multiple, pisahkan dengan koma, gunakan "all" untuk semua)
          required: false
          schema:
            type: string
            example: elektronik,perabot
        - name: userItemStatus
          in: query
          description: "Filter status barter milik user **(value: waiting_for_request, waiting_for_confirmation, confirmed, completed, failed) atau all**"
          required: false
          schema:
            type: string
            example: waiting_for_request,waiting_for_confirmation
        - name: otherItemStatus
          in: query
          description: "Filter status: **(value: request_submitted,confirmed,completed,failed) atau all**"
          required: false
          schema:
            type: string
            example: request_submitted,confirmed
        - name: ownership
          in: query
          description: "Pilih data yang diambil: **my_items, other_items (pisahkan dengan koma, default: all)**"
          required: false
          schema:
            type: string
            example: my_items,other_items
        - name: userItemPage
          in: query
          description: "Halaman data barter milik user (default: 1)"
          required: false
          schema:
            type: integer
            default: 1
        - name: userItemSize
          in: query
          description: "Jumlah data per halaman barter milik user (default: 10)"
          required: false
          schema:
            type: integer
            default: 10
        - name: otherItemPage
          in: query
          description: "Halaman data other items (default: 1)"
          required: false
          schema:
            type: integer
            default: 1
        - name: otherItemSize
          in: query
          description: "Jumlah data per halaman other items (default: 10)"
          required: false
          schema:
            type: integer
            default: 10
      responses:
        '200':
          description: Berhasil mendapatkan riwayat barter user
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: Berhasil mendapatkan riwayat barter
                  meta:
                    type: object
                    properties:
                      my_items:
                        type: object
                        properties:
                          total:
                            type: integer
                            example: 12
                          page:
                            type: integer
                            example: 1
                          size:
                            type: integer
                            example: 10
                          totalPages:
                            type: integer
                            example: 2
                      other_items:
                        type: object
                        properties:
                          total:
                            type: integer
                            example: 5
                          page:
                            type: integer
                            example: 1
                          size:
                            type: integer
                            example: 10
                          totalPages:
                            type: integer
                            example: 1
                  data:
                    type: object
                    properties:
                      my_items:
                        type: array
                        items:
                          type: object
                          properties:
                            id:
                              type: integer
                            item_name:
                              type: string
                            description:
                              type: string
                            images:
                              type: array
                              items:
                                type: string
                                format: uri
                            category:
                              type: object
                              nullable: true
                              properties:
                                id:
                                  type: integer
                                name:
                                  type: string
                            status:
                              type: object
                              nullable: true
                              properties:
                                id:
                                  type: integer
                                status:
                                  type: string
                                updated_at:
                                  type: string
                                  format: date-time
                      other_items:
                        type: array
                        items:
                          type: object
                          properties:
                            id:
                              type: integer
                            item_name:
                              type: string
                            description:
                              type: string
                            images:
                              type: array
                              items:
                                type: string
                                format: uri
                            category:
                              type: object
                              nullable: true
                              properties:
                                id:
                                  type: integer
                                name:
                                  type: string
                            status:
                              type: object
                              nullable: true
                              properties:
                                id:
                                  type: integer
                                status:
                                  type: string
                                updated_at:
                                  type: string
                                  format: date-time
                            user:
                              type: object
                              nullable: true
                              properties:
                                id:
                                  type: integer
                                profile_picture:
                                  type: string
                                  format: uri
                                username:
                                  type: string
                                fullname:
                                  type: string
                            address:
                              type: object
                              nullable: true
                              properties:
                                id:
                                  type: integer
                                address:
                                  type: string
                                latitude:
                                  type: number
                                longitude:
                                  type: number
                            distance:
                              type: number
                              nullable: true
        "401":
          description: Unauthorized (token tidak valid/expired)
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  message:
                    type: string
                    example: "Unauthorized. Need Authorization Token"
                  errors:
                    type: object
                    properties:
                      token:
                        type: string
                        example: null
        
  /api/users/current/barters/{barterId}:
    get:
      tags:
        - Barter
      summary: Mendapatkan detail postingan barter milik user sendiri (my_items) beserta incoming request
      description: |
        Mengambil detail postingan barter milik user saat ini berdasarkan barterId, beserta daftar incoming_request dari user lain.
      parameters:
        - name: barterId
          in: path
          required: true
          description: ID barter milik user login
          schema:
            type: integer
        - name: Authorization
          in: header
          required: true
          schema:
            type: string
          description: JWT token (Bearer)
        - name: Accept-Language
          in: header
          required: false
          schema:
            type: string
          description: Bahasa response (en/id)
        - name: Content-Type
          in: header
          required: true
          schema:
            type: string
            example: application/json
          description: Tipe konten yang dikirim dalam format application/json
      responses:
        '200':
          description: Berhasil mendapatkan detail barter milik user sendiri
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: Berhasil mendapatkan detail barter
                  data:
                    type: object
                    properties:
                      id:
                        type: integer
                        example: 10
                      item_name:
                        type: string
                        example: "Sepeda Bekas"
                      description:
                        type: string
                        example: "Sepeda bekas, masih layak pakai, warna biru."
                      category:
                        type: object
                        properties:
                          id:
                            type: integer
                            example: 1
                          name:
                            type: string
                            example: "Sepeda"
                      status:
                        type: string
                        example: "waiting_for_confirmation"
                      address:
                        type: object
                        properties:
                          id:
                            type: integer
                            example: 2
                          name:
                            type: string
                            example: Rumah
                          address:
                            type: string
                            example: "Jl. Mawar No. 1 Bandung, Jawa Barat, Indonesia"
                          latitude:
                            type: number
                            example: -6.123
                          longitude:
                            type: number
                            example: 106.123
                      images:
                        type: array
                        items:
                          type: string
                          format: uri
                        example:
                          - "https://..."
                          - "https://..."
                      status_histories:
                        type: array
                        items:
                          type: object
                          properties:
                            id:
                              type: integer
                              example: 1
                            status:
                              type: string
                              example: "waiting_for_request"
                            status_detail:
                              type: string
                              example: "Menunggu permintaan barter"
                            updated_at:
                              type: string
                              format: date-time
                              example: "2025-08-26T10:00:00.000Z"
                      incoming_request:
                        type: array
                        items:
                          type: object
                          properties:
                            id:
                              type: integer
                            item_name:
                              type: string
                            description:
                              type: string
                            images:
                              type: array
                              items:
                                type: string
                                format: uri
                            category:
                              type: object
                              nullable: true
                              properties:
                                id:
                                  type: integer
                                name:
                                  type: string
                            status:
                              type: object
                              nullable: true
                              properties:
                                id:
                                  type: integer
                                status:
                                  type: string
                                updated_at:
                                  type: string
                                  format: date-time
                            user:
                              type: object
                              nullable: true
                              properties:
                                id:
                                  type: integer
                                profile_picture:
                                  type: string
                                  format: uri
                                username:
                                  type: string
                                fullname:
                                  type: string
                            address:
                              type: object
                              nullable: true
                              properties:
                                id:
                                  type: intege
                                name:
                                  type: string
                                address:
                                  type: string
                                latitude:
                                  type: number
                                longitude:
                                  type: number
                            distance:
                              type: number
                              nullable: true
        '400':
          description: barterId tidak valid
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  message:
                    type: string
                    example: Barter ID bukan angka
                  errors:
                    type: object
                    properties:
                      token:
                        type: string
                        example: null
        "401":
          description: Unauthorized (token tidak valid/expired)
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  message:
                    type: string
                    example: "Unauthorized. Need Authorization Token"
                  errors:
                    type: object
                    properties:
                      token:
                        type: string
                        example: null
        '404':
          description: Barter tidak ditemukan
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  message:
                    type: string
                    example: Barter tidak ditemukan
                  errors:
                    type: object
                    properties:
                      token:
                        type: string
                        example: null

  /api/users/current/barter-requests/{requestId}:
    get:
      tags:
        - Barter
      summary: Mendapatkan detail incoming request (other_items) milik user login
      description: |
        Mengambil detail barter application (incoming request) milik user login, beserta data barter yang diajukan (barter_with).
      parameters:
        - name: requestId
          in: path
          required: true
          schema: { type: integer }
          description: ID barter application (other_items)
        - name: Authorization
          in: header
          required: true
          schema:
            type: string
          description: JWT token (Bearer)
        - name: Accept-Language
          in: header
          required: false
          schema:
            type: string
          description: Bahasa response (en/id)
        - name: Content-Type
          in: header
          required: true
          schema:
            type: string
            example: application/json
          description: Tipe konten yang dikirim dalam format application/json
      responses:
        '200':
          description: Berhasil mendapatkan detail incoming request
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: Berhasil mendapatkan detail incoming request
                  data:
                    type: object
                    properties:
                      id:
                        type: integer
                      item_name:
                        type: string
                      description:
                        type: string
                      images:
                        type: array
                        items:
                          type: string
                          format: uri
                      category:
                        type: object
                        properties:
                          id:
                            type: integer
                          name:
                            type: string
                      status:
                        type: string
                      status_histories:
                        type: array
                        items:
                          type: object
                          properties:
                            id:
                              type: integer
                            status:
                              type: string
                            status_detail:
                              type: string
                            updated_at:
                              type: string
                              format: date-time
                      address:
                        type: object
                        properties:
                          id:
                            type: integer
                            example: 1
                          name:
                            type: string
                            example: Rumah
                          address:
                            type: string
                            example: Permata Buah Batu, Lengkong, Bojongsoang, Kabupaten Bandung, Jawa Barat, Jawa, Indonesia
                          latitude:
                            type: number
                            example: -6.9724402
                          longitude:
                            type: number
                            example: 107.638862
                      distance:
                        type: number
                      user:
                        type: object
                        properties:
                          id:
                            type: integer
                          profile_picture:
                            type: string
                            format: uri
                          username:
                            type: string
                          fullname:
                            type: string
                      barter_with:
                        type: object
                        properties:
                          id:
                            type: integer
                          item_name:
                            type: string
                          description:
                            type: string
                          images:
                            type: array
                            items:
                              type: string
                              format: uri
                          category:
                            type: object
                            properties:
                              id:
                                type: integer
                              name:
                                type: string
                          status:
                            type: object
                            properties:
                              id:
                                type: integer
                              status:
                                type: string
                              updated_at:
                                type: string
                                format: date-time
                          address:
                            type: object
                            properties:
                              id:
                                type: integer
                              name:
                                type: string
                              address:
                                type: string
                              latitude:
                                type: number
                              longitude:
                                type: number
        '400':
          description: requestId tidak valid
        '401':
          description: Unauthorized
        '403':
          description: Forbidden (bukan milik user login)
        '404':
          description: Data tidak ditemukan
    
  /api/users/current/barters/{barterId}/mark-as-completed:
    post:
      tags:
        - Barter
      summary: Tandai barter sebagai selesai (mark as completed)
      description: |
        Hanya dapat dilakukan oleh pemilik barterId jika status barter sudah confirmed dan salah satu incomin requestnya confirmed
        Akan mengubah status barter dan barter request terkait menjadi completed.
      parameters:
        - name: barterId
          in: path
          required: true
          schema: { type: integer }
          description: ID barter milik user login
        - name: requestId
          in: path
          required: true
          schema: { type: integer }
          description: ID barter request yang sudah di-accept
        - name: Authorization
          in: header
          required: true
          schema:
            type: string
          description: JWT token (Bearer)
        - name: Accept-Language
          in: header
          required: false
          schema:
            type: string
          description: Bahasa response (en/id)
        - name: Content-Type
          in: header
          required: true
          schema:
            type: string
            example: application/json
      responses:
        '200':
          description: Berhasil menandai barter sebagai selesai
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: Barter marked as completed successfully
        '400':
          description: Barter belum dikonfirmasi atau request belum dikonfirmasi
        '401':
          description: Unauthorized
        '404':
          description: Data tidak ditemukan

  /api/users/current/barters/{barterId}/requests/{requestId}:
    get:
      tags:
        - Barter
      summary: Mendapatkan detail incoming request user lain pada postingan barter milik user sendiri
      description: |
        Mengambil detail request barter (incoming request) dari user lain pada postingan barter milik user yang sedang login, beserta detail item barter milik user pada field barter_with.
      parameters:
        - name: barterId
          in: path
          required: true
          description: ID barter milik user login
          schema:
            type: integer
        - name: requestId
          in: path
          required: true
          description: ID barter request yang ingin diambil detailnya
          schema:
            type: integer
        - name: Authorization
          in: header
          required: true
          schema:
            type: string
          description: JWT token (Bearer)
        - name: Accept-Language
          in: header
          required: false
          schema:
            type: string
          description: Bahasa response (en/id)
      responses:
        '200':
          description: Berhasil mendapatkan detail incoming request
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: Berhasil mendapatkan detail request barter yang masuk
                  data:
                    type: object
                    properties:
                      id:
                        type: integer
                      item_name:
                        type: string
                      description:
                        type: string
                      images:
                        type: array
                        items:
                          type: string
                          format: uri
                      category:
                        type: object
                        nullable: true
                        properties:
                          id:
                            type: integer
                          name:
                            type: string
                      status:
                        type: object
                        nullable: true
                        properties:
                          id:
                            type: integer
                          status:
                            type: string
                          updated_at:
                            type: string
                            format: date-time
                      user:
                        type: object
                        nullable: true
                        properties:
                          id:
                            type: integer
                          profile_picture:
                            type: string
                            format: uri
                            example: "http://..."
                          username:
                            type: string
                          fullname:
                            type: string
                      address:
                        type: object
                        nullable: true
                        properties:
                          id:
                            type: integer
                          address:
                            type: string
                          latitude:
                            type: number
                          longitude:
                            type: number
                      distance:
                        type: number
                        nullable: true
                      phone: 
                        type: string
                        example: "+6281122334455"
                      barter_with:
                        type: object
                        properties:
                          id:
                            type: integer
                          item_name:
                            type: string
                          description:
                            type: string
                          images:
                            type: array
                            items:
                              type: string
                              format: uri
                              example: "http://..."
                          category:
                            type: object
                            nullable: true
                            properties:
                              id:
                                type: integer
                              name:
                                type: string
                              status:
                                type: string
                          status_histories:
                            type: array
                            items:
                              type: object
                              properties:
                                id:
                                  type: integer
                                status:
                                  type: string
                                status_detail:
                                  type: string
                                updated_at:
                                  type: string
                                  format: date-time
        '400':
          description: barterId atau requestId tidak valid
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  message:
                    type: string
                    example: Barter ID bukan angka || Request ID bukan angka
                  errors:
                    type: object
                    properties:
                      token:
                        type: string
                        example: null
        "401":
          description: Unauthorized (token tidak valid/expired)
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  message:
                    type: string
                    example: "Unauthorized. Need Authorization Token"
                  errors:
                    type: object
                    properties:
                      token:
                        type: string
                        example: null
        '404':
          description: Data tidak ditemukan
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  message:
                    type: string
                    example: Barter tidak ditemukan || Request tidak ditemukan
                  errors:
                    type: object
                    properties:
                      token:
                        type: string
                        example: null

  /api/users/current/barters/{barterId}/requests/{requestId}/process:
    post:
      tags:
        - Barter
      summary: Proses (accept/decline) incoming request pada postingan barter milik user
      description: |
        Accept akan mengubah status requestId menjadi confirmed dan semua request lain pada barterId yang sama menjadi failed.
        Decline akan mengubah status requestId menjadi failed dengan alasan yang diisi user.
        Aksi ini dapat diproses hanya jika status barterId adalah waiting_for_confirmation & status requestId adalah request_submitted
      parameters:
        - name: barterId
          in: path
          required: true
          schema:
            type: integer
          description: ID barter milik user login
        - name: requestId
          in: path
          required: true
          schema:
            type: integer
          description: ID barter request yang ingin diproses
        - name: Authorization
          in: header
          required: true
          schema:
            type: string
          description: JWT token (Bearer)
        - name: Accept-Language
          in: header
          required: false
          schema:
            type: string
          description: Bahasa response (en/id)
        - name: Content-Type
          in: header
          required: true
          schema:
            type: string
            example: application/json
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                action:
                  type: string
                  enum: [accept, decline]
                  description: Pilihan aksi, "accept" untuk menerima, "decline" untuk menolak
                decline_reason:
                  type: string
                  nullable: true
                  description: Alasan penolakan, wajib jika action=decline
              required:
                - action
            examples:
              accept:
                summary: Accept request
                value:
                  action: accept
              decline:
                summary: Decline request
                value:
                  action: decline
                  decline_reason: "Maaf, barang sudah tidak tersedia"
      responses:
        '200':
          description: Berhasil memproses request
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: Barter request accepted successfully
        '400':
          description: Bad request (action tidak valid atau alasan decline kosong)
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  message:
                    type: string
                    example: Nilai action harus accept atau decline
                  errors:
                    type: object
                    nullable: true
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  message:
                    type: string
                    example: Unauthorized. Need Authorization Token
                  errors:
                    type: object
                    nullable: true
        '404':
          description: Data tidak ditemukan
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  message:
                    type: string
                    example: Barter not found
                  errors:
                    type: object
                    nullable: true

  /api/borrows:
    get:
      tags:
        - Borrow
      summary: Mendapatkan daftar postingan borrow milik user lain (Discover borrow)
      description: |
        Endpoint untuk menampilkan daftar postingan borrow milik user lain (bukan milik user yang sedang login).
        Mendukung pencarian, filter kategori, filter lokasi (maxDistance atau nama lokasi), filter durasi (from & to, atau jumlah hari), dan sorting.
      parameters:
        - name: Authorization
          in: header
          required: true
          schema:
            type: string
          description: JWT token (Bearer)
        - name: Accept-Language
          in: header
          required: false
          schema:
            type: string
          description: Bahasa response (en/id)
        - name: search
          in: query
          required: false
          schema:
            type: string
          description: Pencarian pada item_name atau description
        - name: category
          in: query
          required: false
          schema:
            type: string
            example: "elektronik,perabot"
          description: Filter berdasarkan nama kategori (bisa multiple, pisahkan dengan koma)
        - name: maxDistance
          in: query
          required: false
          schema:
            type: number
            example: 2
          description: Filter postingan dengan jarak maksimum (km) dari address user login. Jika diisi, parameter location diabaikan.
        - name: location
          in: query
          required: false
          schema:
            type: string
            example: "Bandung"
          description: Filter berdasarkan nama lokasi. **Diabaikan jika maxDistance diisi**.
        - name: from
          in: query
          required: false
          schema:
            type: string
            format: date
            example: "2025-08-31"
          description: Tanggal mulai interval pencarian (format YYYY-MM-DD)
        - name: to
          in: query
          required: false
          schema:
            type: string
            format: date
            example: "2025-09-21"
          description: Tanggal akhir interval pencarian (format YYYY-MM-DD)
        - name: days
          in: query
          required: false
          schema:
            type: integer
            example: 7
          description: Filter postingan dengan minimal durasi available (dalam hari). **Diabaikan jika from & to diisi.**
        - name: sortBy
          in: query
          required: false
          schema:
            type: string
            enum: [relevance, newest, nearest, borrowing_duration]
          description: Urutan hasil (relevance, newest, nearest, borrowing_duration). Default relevance
        - name: page
          in: query
          required: false
          schema:
            type: integer
            default: 1
          description: "Halaman (default: 1)"
        - name: size
          in: query
          required: false
          schema:
            type: integer
            default: 10
          description: "Jumlah data per halaman (default: 10)"
      responses:
        "200":
          description: Daftar postingan borrow berhasil diambil
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: Berhasil mendapatkan daftar peminjaman
                  meta:
                    type: object
                    properties:
                      total:
                        type: integer
                        example: 100
                      page:
                        type: integer
                        example: 1
                      size:
                        type: integer
                        example: 10
                      totalPages:
                        type: integer
                        example: 10
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: integer
                          example: 10
                        item_name:
                          type: string
                          example: Kamera Canon EOS
                        description:
                          type: string
                          example: Kamera DSLR, kondisi sangat baik, lengkap dengan lensa kit.
                        category:
                          type: object
                          properties:
                            id:
                              type: integer
                              example: 2
                            name:
                              type: string
                              example: Elektronik
                        address:
                          type: object
                          properties:
                            id:
                              type: integer
                              example: 3
                            address:
                              type: string
                              example: "Jl. Mawar No. 1, Kabupaten Bandung, Indonesia"
                            latitude:
                              type: number
                              example: -6.123
                            longitude:
                              type: number
                              example: 106.123
                        distance:
                          type: number
                          example: 2.5
                          description: "Jarak terdekat (km) dari daftar alamat user login"
                        user:
                          type: object
                          properties:
                            id:
                              type: integer
                              example: 5
                            profile_picture:
                              type: string
                              example: "https://.../username.png"
                            username:
                              type: string
                              example: "budi123"
                            fullname:
                              type: string
                              example: "Budi Santoso"
                        images:
                          type: array
                          items:
                            type: string
                            format: uri
                          example:
                            - https://...
                            - https://...
                        duration_from:
                          type: string
                          format: date-time
                          example: "2025-09-01T00:00:00.000Z"
                        duration_to:
                          type: string
                          format: date-time
                          example: "2025-09-10T00:00:00.000Z"
                        borrowing_duration:
                          type: number
                          example: 9
                          description: "Durasi available (hari)"
        "401":
          description: Unauthorized (token tidak valid/expired)
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  message:
                    type: string
                    example: "Unauthorized. Need Authorization Token"
                  errors:
                    type: object
                    properties:
                      token:
                        type: string
                        example: null
        "404":
          description: User tidak punya alamat
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  message:
                    type: string
                    example: User has no address

    post:
      tags:
        - Borrow
      summary: Membuat postingan borrow baru
      description: |
        Endpoint untuk membuat postingan peminjaman barang (borrow). User mengisi data item, kategori, alamat, kontak, durasi available, dan upload gambar.
        Status awal borrow adalah `waiting_for_request`.
      parameters:
        - name: Authorization
          in: header
          required: true
          schema:
            type: string
          description: JWT token (Bearer)
        - name: Accept-Language
          in: header
          required: false
          schema:
            type: string
          description: Bahasa response (en/id)
        - name: Content-Type
          in: header
          required: true
          schema:
            type: string
            example: multipart/form-data
          description: Tipe konten yang dikirim dalam format multipart/form-data karena ada file yang disertakan
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                item_name:
                  type: string
                  maxLength: 255
                  example: Kamera Canon EOS
                  description: Nama barang yang ingin dipinjamkan
                description:
                  type: string
                  maxLength: 255
                  example: Kamera DSLR, kondisi sangat baik, lengkap dengan lensa kit.
                  description: Deskripsi barang
                category_id:
                  type: integer
                  example: 2
                  description: ID kategori barang
                address_id:
                  type: integer
                  example: 3
                  description: ID alamat user
                phone_id:
                  type: integer
                  example: 4
                  description: ID nomor kontak user
                duration_from:
                  type: string
                  format: date
                  example: "2025-09-01"
                  description: Tanggal mulai barang tersedia untuk dipinjam (format YYYY-MM-DD)
                duration_to:
                  type: string
                  format: date
                  example: "2025-09-10"
                  description: Tanggal akhir barang tersedia untuk dipinjam (format YYYY-MM-DD)
                images:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: Daftar file gambar barang (maksimal 5 gambar)
              required:
                - item_name
                - description
                - category_id
                - address_id
                - phone_id
                - duration_from
                - duration_to
                - images
      responses:
        "201":
          description: Borrow berhasil dibuat
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: Borrow created successfully
                  data:
                    type: object
                    properties:
                      id:
                        type: integer
                        example: 10
                      item_name:
                        type: string
                        example: Kamera Canon EOS
                      description:
                        type: string
                        example: Kamera DSLR, kondisi sangat baik, lengkap dengan lensa kit.
                      category:
                        type: object
                        properties:
                          id:
                            type: integer
                            example: 2
                          name:
                            type: string
                            example: Elektronik
                      address_id:
                        type: integer
                        example: 3
                      phone_id:
                        type: integer
                        example: 4
                      duration_from:
                        type: string
                        format: date-time
                        example: "2025-09-01T00:00:00.000Z"
                      duration_to:
                        type: string
                        format: date-time
                        example: "2025-09-10T00:00:00.000Z"
                      images:
                        type: array
                        items:
                          type: string
                          format: uri
                        example:
                          - https://...
                          - https://...
                      status:
                        type: string
                        example: Waiting For Request
        "400":
          description: Validasi gagal atau request tidak sesuai (seperti durasi from & to tidak valid)
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  message:
                    type: string
                    example: Validasi gagal
                  errors:
                    type: object
                    example:
                      item_name:
                        - Kolom item_name wajib diisi
                      description:
                        - Kolom description wajib diisi
        "401":
          description: Unauthorized (token tidak valid/expired)
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  message:
                    type: string
                    example: Unauthorized. Need Authorization Token
                  errors:
                    type: object
                    properties:
                      token:
                        type: string
                        example: null
        "404":
          description: Data kategori/alamat/phone tidak ditemukan atau tidak dimiliki user
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  message:
                    type: string
                    example: Data tidak ditemukan

  /api/borrows/{borrowId}:
    get:
      tags:
        - Borrow
      summary: Mendapatkan detail postingan borrow user lain
      description: |
        Mengambil detail postingan borrow milik user lain berdasarkan borrowId.
      parameters:
        - name: borrowId
          in: path
          required: true
          schema:
            type: integer
          description: ID borrow yang ingin diambil detailnya
        - name: Authorization
          in: header
          required: true
          schema:
            type: string
          description: JWT token (Bearer)
        - name: Accept-Language
          in: header
          required: false
          schema:
            type: string
          description: Bahasa response (en/id)
      responses:
        '200':
          description: Berhasil mendapatkan detail borrow
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: Berhasil mendapatkan detail borrow
                  data:
                    type: object
                    properties:
                      id:
                        type: integer
                        example: 1
                      item_name:
                        type: string
                        example: "Kamera Canon EOS"
                      description:
                        type: string
                        example: "Kamera DSLR, kondisi sangat baik, lengkap dengan lensa kit."
                      images:
                        type: array
                        items:
                          type: string
                          format: uri
                        example:
                          - "https://..."
                          - "https://..."
                      category:
                        type: object
                        nullable: true
                        properties:
                          id:
                            type: integer
                            example: 2
                          name:
                            type: string
                            example: "Elektronik"
                      status:
                        type: object
                        nullable: true
                        properties:
                          id:
                            type: integer
                            example: 1
                          status:
                            type: string
                            example: "Waiting For Request"
                          updated_at:
                            type: string
                            format: date-time
                            example: "2025-08-28T10:27:14.759Z"
                      user:
                        type: object
                        nullable: true
                        properties:
                          id:
                            type: integer
                            example: 5
                          profile_picture:
                            type: string
                            format: uri
                            example: "https://.../username.png"
                          username:
                            type: string
                            example: "budi123"
                          fullname:
                            type: string
                            example: "Budi Santoso"
                      address:
                        type: object
                        nullable: true
                        properties:
                          id:
                            type: integer
                            example: 3
                          address:
                            type: string
                            example: "Jl. Mawar No. 1, Kabupaten Bandung, Indonesia"
                          latitude:
                            type: number
                            example: -6.123
                          longitude:
                            type: number
                            example: 106.123
                      distance:
                        type: number
                        nullable: true
                        example: 2.5
                      phone:
                        type: object
                        nullable: true
                        properties:
                          id:
                            type: integer
                            example: 4
                          number:
                            type: string
                            example: "08123456789"
                      duration_from:
                        type: string
                        format: date-time
                        example: "2025-09-01T00:00:00.000Z"
                      duration_to:
                        type: string
                        format: date-time
                        example: "2025-09-10T00:00:00.000Z"
                      borrowing_duration:
                          type: number
                          example: 9
                          description: "Durasi available (hari)"
        '400':
          description: Borrow id
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  message:
                    type: string
                    example: Borrow ID bukan angka
                  errors:
                    type: object
                    properties:
                      token:
                        type: string
                        example: null
        "401":
          description: Unauthorized (token tidak valid/expired)
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  message:
                    type: string
                    example: "Unauthorized. Need Authorization Token"
                  errors:
                    type: object
                    properties:
                      token:
                        type: string
                        example: null
        '404':
          description: Data borrow tidak ditemukan
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  message:
                    type: string
                    example: "Borrow not found"
                  errors:
                    type: object
                    properties:
                      token:
                        type: string
                        example: null

  /api/borrows/{borrowId}/request:
    post:
      tags:
        - Borrow
      summary: Ajukan Permintaan Pinjam ke Postingan Borrow User Lain
      description: |
        Endpoint untuk mengajukan permintaan pinjam ke postingan borrow milik user lain.
        - Hanya bisa mengajukan ke postingan borrow milik user lain.
        - Status awal request adalah `request_submitted`.
      parameters:
        - name: borrowId
          in: path
          required: true
          schema:
            type: integer
          description: ID borrow yang akan diajukan permintaan pinjam
        - name: Authorization
          in: header
          required: true
          schema:
            type: string
          description: JWT token (Bearer)
        - name: Accept-Language
          in: header
          required: false
          schema:
            type: string
          description: Bahasa response (en/id)
        - name: Content-Type
          in: header
          required: true
          schema:
            type: string
            example: application/json
          description: Tipe konten yang dikirim dalam format application/json
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                duration_from:
                  type: string
                  format: date
                  example: "2025-09-05"
                  description: Tanggal mulai pinjam (format YYYY-MM-DD)
                duration_to:
                  type: string
                  format: date
                  example: "2025-09-08"
                  description: Tanggal akhir pinjam (format YYYY-MM-DD)
                reason:
                  type: string
                  maxLength: 255
                  example: "Saya butuh kamera untuk acara kampus."
                  description: Pesan tambahan
                address_id:
                  type: int
                  example: 1
                phone_id:
                  type: int
                  example: 1
              required:
                - duration_from
                - duration_to
                - reason
                - address_id
                - phone_id
      responses:
        "201":
          description: Permintaan pinjam berhasil diajukan
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: Permintaan pinjam berhasil diajukan
                  data:
                    type: object
                    properties:
                      id:
                        type: integer
                        description: ID borrow application
                        example: 12
                      borrow_id:
                        type: integer
                        description: ID postingan borrow yang diajukan
                        example: 10
                      reason:
                        type: string
                        description: Reason for borrowing yang diisikan user
                        example: "Saya butuh kamera untuk acara kampus"
                      duration_from:
                        type: string
                        format: date
                        example: "2025-09-05"
                      duration_to:
                        type: string
                        format: date
                        example: "2025-09-08"
                      status:
                        type: string
                        example: Request Submitted
                        description: Status borrow request
        "400":
          description: Validasi gagal atau request tidak sesuai (tanggal tidak valid, dsb)
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  message:
                    type: string
                    example: Validasi gagal
                  errors:
                    type: object
                    example:
                      reason: "Kolom reason wajib diisi"
                      borrow_from: "Tanggal mulai pinjam wajib diisi"
                      borrow_to: "Tanggal akhir pinjam wajib diisi"
        "401":
          description: Unauthorized (token tidak valid/expired)
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  message:
                    type: string
                    example: Unauthorized. Need Authorization Token
                  errors:
                    type: object
                    properties:
                      token:
                        type: string
                        example: null
        "403":
          description: Tidak bisa request ke postingan sendiri
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  message:
                    type: string
                    example: Tidak bisa mengajukan permintaan ke postingan sendiri
                  errors:
                    type: object
                    example: null
        "404":
          description: Data borrow tidak ditemukan
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  message:
                    type: string
                    example: Data borrow tidak ditemukan
                  errors:
                    type: object
                    example: null

  /api/users/current/borrows:
    get:
      tags:
        - Borrow
      summary: Mendapatkan riwayat borrow user (posting & incoming request)
      description: |
        Mengambil daftar borrow milik user saat ini (my_items) dan daftar request user saat ini (other_items), dengan filter kategori, status, search, dan pagination terpisah.
      parameters:
        - name: Authorization
          in: header
          required: true
          schema:
            type: string
          description: JWT token (Bearer)
        - name: Accept-Language
          in: header
          required: false
          schema:
            type: string
          description: Bahasa response (en/id)
        - name: search
          in: query
          description: Pencarian nama item atau deskripsi (optional)
          required: false
          schema:
            type: string
        - name: category
          in: query
          description: Filter berdasarkan nama kategori (bisa multiple, pisahkan dengan koma, gunakan "all" untuk semua)
          required: false
          schema:
            type: string
            example: elektronik,perabot
        - name: userItemStatus
          in: query
          description: "Filter status borrow milik user **(Value: waiting_for_request, waiting_for_confirmation, confirmed, lent, returned, overdue, completed, cancelled) atau all**"
          required: false
          schema:
            type: string
            example: waiting_for_request,waiting_for_confirmation
        - name: otherItemStatus
          in: query
          description: "Filter status other_items **(Value: request_submitted,confirmed,borrowed,returned,overdue,completed,cancelled) atau all**"
          required: false
          schema:
            type: string
            example: request_submitted,confirmed
        - name: ownership
          in: query
          description: "Pilih data yang diambil: **my_items, other_items, (pisahkan dengan koma, default: all)**"
          required: false
          schema:
            type: string
            example: my_items,other_requests
        - name: userItemPage
          in: query
          description: "Halaman data borrow milik user (default: 1)"
          required: false
          schema:
            type: integer
            default: 1
        - name: userItemSize
          in: query
          description: "Jumlah data per halaman borrow milik user (default: 10)"
          required: false
          schema:
            type: integer
            default: 10
        - name: otherItemPage
          in: query
          description: "Halaman data other_items (default: 1)"
          required: false
          schema:
            type: integer
            default: 1
        - name: otherItemSize
          in: query
          description: "Jumlah data per halaman other_items (default: 10)"
          required: false
          schema:
            type: integer
            default: 10
      responses:
        '200':
          description: Berhasil mendapatkan riwayat borrow user
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: Berhasil mendapatkan riwayat borrow
                  meta:
                    type: object
                    properties:
                      my_items:
                        type: object
                        properties:
                          total: { type: integer, example: 12 }
                          page: { type: integer, example: 1 }
                          size: { type: integer, example: 10 }
                          totalPages: { type: integer, example: 2 }
                      other_items:
                        type: object
                        properties:
                          total: { type: integer, example: 5 }
                          page: { type: integer, example: 1 }
                          size: { type: integer, example: 10 }
                          totalPages: { type: integer, example: 1 }
                  data:
                    type: object
                    properties:
                      my_items:
                        type: array
                        items:
                          type: object
                          properties:
                            id: { type: integer }
                            item_name: { type: string }
                            description: { type: string }
                            images:
                              type: array
                              items: { type: string, format: uri }
                            category:
                              type: object
                              nullable: true
                              properties:
                                id: { type: integer }
                                name: { type: string }
                            status:
                              type: object
                              nullable: true
                              properties:
                                id: { type: integer }
                                status: { type: string }
                                updated_at: { type: string, format: date-time }
                            duration_from: { type: string, format: date-time }
                            duration_to: { type: string, format: date-time }
                      other_items:
                        type: array
                        items:
                          type: object
                          properties:
                            id: { type: integer }
                            item_name: { type: string }
                            description: { type: string }
                            images:
                              type: array
                              items: { type: string, format: uri }
                            category:
                              type: object
                              nullable: true
                              properties:
                                id: { type: integer }
                                name: { type: string }
                            status:
                              type: object
                              nullable: true
                              properties:
                                id: { type: integer }
                                status: { type: string }
                                updated_at: { type: string, format: date-time }
                            address:
                              type: object
                              nullable: true
                              properties:
                                id: { type: integer }
                                address: { type: string }
                                latitude: { type: number }
                                longitude: { type: number }
                            distance: { type: number }
                            user:
                              type: object
                              nullable: true
                              properties:
                                id: { type: integer }
                                profile_picture: { type: string, format: uri }
                                fullname: { type: string }
                                username: { type: string }
        '401':
          description: Unauthorized (token tidak valid/expired)
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: false }
                  message: { type: string, example: "Unauthorized. Need Authorization Token" }
                  errors:
                    type: object
                    properties:
                      token: { type: string, example: null }

  /api/users/current/borrows/{borrowId}:
    get:
      tags:
        - Borrow
      summary: Mendapatkan detail postingan borrow user saat ini
      description: |
        Mengambil detail postingan borrow milik user saat ini, beserta daftar incoming request dari user lain ke postingan ini.
      parameters:
        - name: borrowId
          in: path
          required: true
          schema:
            type: integer
          description: ID dari postingan borrow yang ingin diambil detailnya
        - name: Authorization
          in: header
          required: true
          schema:
            type: string
          description: JWT token (Bearer)
        - name: Accept-Language
          in: header
          required: false
          schema:
            type: string
          description: Bahasa response (en/id)
      responses:
        '200':
          description: Detail borrow post berhasil diambil
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      id:
                        type: integer
                        example: 10
                      item_name:
                        type: string
                        example: "Kamera DSLR Canon"
                      description:
                        type: string
                        example: "Kamera masih mulus, lengkap dengan lensa kit."
                      category:
                        type: object
                        properties:
                          id:
                            type: integer
                            example: 2
                          name:
                            type: string
                            example: "Elektronik"
                      images:
                        type: array
                        items:
                          type: string
                        example: ["https://...", "https://..."]
                      status:
                        type: string
                        example: "waiting_for_request"
                      status_histories:
                        type: array
                        items:
                          type: object
                          properties:
                            id:
                              type: integer
                              example: 1
                            status:
                              type: string
                              example: "waiting_for_request"
                            status_detail:
                              type: string
                              example: "Menunggu permintaan peminjam"
                            updated_at:
                              type: string
                              format: date-time
                              example: "2025-09-03T10:00:00.000Z"
                      address:
                        type: object
                        properties:
                          id:
                            type: integer
                            example: 3
                          name:
                            type: string
                            example: Rumah
                          address:
                            type: string
                            example: "Jl. Merdeka No. 1, Bandung"
                          latitude:
                            type: number
                            example: -6.914744
                          longitude:
                            type: number
                            example: 107.609810
                      duration_from:
                        type: string
                        format: date
                        example: "2025-09-05"
                      duration_to:
                        type: string
                        format: date
                        example: "2025-09-12"
                      borrowing_duration:
                        type: integer
                        example: 7
                      incoming_requests:
                        type: array
                        items:
                          type: object
                          properties:
                            id:
                              type: integer
                              example: 21
                            reason:
                              type: string
                              example: "Untuk tugas kuliah"
                            address:
                              type: object
                              properties:
                                id:
                                  type: integer
                                  example: 5
                                name:
                                  type: string
                                  example: Rumah
                                address:
                                  type: string
                                  example: "Jl. Asia Afrika No. 2, Bandung ..."
                                latitude:
                                  type: number
                                  example: -6.921
                                longitude:
                                  type: number
                                  example: 107.607
                            distance:
                              type: number
                              example: 2.5
                            user:
                              type: object
                              properties:
                                id:
                                  type: integer
                                  example: 7
                                profile_picture:
                                  type: string
                                  example: https://...
                                fullname:
                                  type: string
                                  example: "Budi"
                                username:
                                  type: string
                                  example: "budi123"
                            status:
                              type: object
                              properties:
                                id:
                                  type: integer
                                  example: 1
                                status:
                                  type: string
                                  example: "pending"
                                updated_at:
                                  type: string
                                  format: date-time
                                  example: "2025-09-03T10:00:00.000Z"
                            duration_from:
                              type: string
                              format: date
                              example: "2025-09-06"
                            duration_to:
                              type: string
                              format: date
                              example: "2025-09-10"
                            borrowing_duration:
                              type: integer
                              example: 4
        '404':
          description: Borrow post tidak ditemukan atau bukan milik user
        '401':
          description: Unauthorized
  
  /api/users/current/borrow-requests/{requestId}:
    get:
      tags:
        - Borrow
      summary: Mendapatkan detail other_items pada user borrow histories
      description: |
        Mengambil detail borrow request yang diajukan oleh user saat ini terhadap suatu item. Hanya dapat mengakses request milik user sendiri.
      parameters:
        - name: requestId
          in: path
          required: true
          schema:
            type: integer
          description: ID dari borrow request yang ingin diambil detailnya
        - name: Authorization
          in: header
          required: true
          schema:
            type: string
          description: JWT token (Bearer)
        - name: Accept-Language
          in: header
          required: false
          schema:
            type: string
          description: Bahasa response (en/id)
      responses:
        '200':
          description: Detail borrow request berhasil diambil
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: Berhasil mendapatkan detail request barter
                  data:
                    type: object
                    properties:
                      id: { type: integer }
                      item_name: { type: string }
                      description: { type: string }
                      images:
                        type: array
                        items: { type: string, format: uri }
                      category:
                        type: object
                        nullable: true
                        properties:
                          id: { type: integer }
                          name: { type: string }
                      status: { type: string }
                      status_histories:
                        type: array
                        items:
                          type: object
                          properties:
                            id: { type: int }
                            status: { type: string }
                            status_detail: { type: string }
                            updated_at: { type: string, format: date-time }
                      address:
                        type: object
                        nullable: true
                        properties:
                          id: { type: integer }
                          name:
                            type: string
                            example: Rumah
                          address: { type: string }
                          latitude: { type: number }
                          longitude: { type: number }
                      distance: { type: number }
                      user:
                        type: object
                        nullable: true
                        properties:
                          id: { type: integer }
                          profile_picture: { type: string, format: uri }
                          fullname: { type: string }
                          username: { type: string }
                      duration_from: { type: string, format: date-time }
                      duration_to: { type: string, format: date-time }
                      borrowing_duration: { type: int } 
                      reason: { type: string } 
                      current_user_address:
                        type: object
                        nullable: true
                        properties:
                          id: { type: integer }
                          name: { type: string }
                          address: { type: string }
                          latitude: { type: number }
                          longitude: { type: number }
        '404':
          description: Borrow request tidak ditemukan atau bukan milik user
        '401':
          description: Unauthorized

  /api/users/current/borrows/{borrowId}/mark-as-lent:
    post:
      tags:
        - Borrow
      summary: Tandai borrow sebagai Lent
      description: |
        Hanya dapat dilakukan oleh pemilik borrowId jika status borrow sudah confirmed dan status salah satu incoming requestnya confirmed.
        Akan mengubah status borrow dan borrow request terkait menjadi Lent dan Borrowed.
      parameters:
        - name: borrowId
          in: path
          required: true
          schema: { type: integer }
          description: ID borrow milik user login
        - name: requestId
          in: path
          required: true
          schema: { type: integer }
          description: ID borrow request
        - name: Authorization
          in: header
          required: true
          schema:
            type: string
          description: JWT token (Bearer)
        - name: Accept-Language
          in: header
          required: false
          schema:
            type: string
          description: Bahasa response (en/id)
        - name: Content-Type
          in: header
          required: true
          schema:
            type: string
            example: application/json
      responses:
        '200':
          description: Berhasil menandai borrow sebagai Lent
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: Mark borrow as lent successfully
        '400':
          description: borrow belum berstatus returned atau request belum berstatus returned
        '401':
          description: Unauthorized
        '404':
          description: Data tidak ditemukan
  
  /api/users/current/borrows/{borrowId}/mark-as-returned:
    post:
      tags:
        - Borrow
      summary: Tandai borrow sebagai Returned
      description: |
        Hanya dapat dilakukan oleh pemilik borrowId jika status borrow sudah Lent dan status salah satu incoming requestnya Borrowed.
        Akan mengubah status borrow dan borrow request terkait menjadi Returned.
      parameters:
        - name: borrowId
          in: path
          required: true
          schema: { type: integer }
          description: ID borrow milik user login
        - name: requestId
          in: path
          required: true
          schema: { type: integer }
          description: ID borrow request
        - name: Authorization
          in: header
          required: true
          schema:
            type: string
          description: JWT token (Bearer)
        - name: Accept-Language
          in: header
          required: false
          schema:
            type: string
          description: Bahasa response (en/id)
        - name: Content-Type
          in: header
          required: true
          schema:
            type: string
            example: application/json
      responses:
        '200':
          description: Berhasil menandai borrow sebagai Returned
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: Mark borrow as returned successfully
        '400':
          description: borrow belum berstatus returned atau request belum berstatus returned
        '401':
          description: Unauthorized
        '404':
          description: Data tidak ditemukan

  /api/users/current/borrows/{borrowId}/mark-as-completed:
    post:
      tags:
        - Borrow
      summary: Tandai borrow sebagai selesai (mark as completed)
      description: |
        Hanya dapat dilakukan oleh pemilik borrowId jika status borrow sudah returned dan status salah satu incoming requestnya returned.
        Akan mengubah status borrow dan borrow request terkait menjadi completed.
      parameters:
        - name: borrowId
          in: path
          required: true
          schema: { type: integer }
          description: ID borrow milik user login
        - name: requestId
          in: path
          required: true
          schema: { type: integer }
          description: ID borrow request
        - name: Authorization
          in: header
          required: true
          schema:
            type: string
          description: JWT token (Bearer)
        - name: Accept-Language
          in: header
          required: false
          schema:
            type: string
          description: Bahasa response (en/id)
        - name: Content-Type
          in: header
          required: true
          schema:
            type: string
            example: application/json
      responses:
        '200':
          description: Berhasil menandai borrow sebagai selesai
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: Mark borrow as completed successfully
        '400':
          description: borrow belum berstatus returned atau request belum berstatus returned
        '401':
          description: Unauthorized
        '404':
          description: Data tidak ditemukan
  
  /api/users/current/borrows/{borrowId}/requests/{requestId}:
    get:
      tags:
        - Borrow
      summary: Mendapatkan detail request yang masuk ke borrowId
      description: |
        Mengambil detail borrow request yang masuk ke postingan borrow milik user saat ini.
      parameters:
        - name: borrowId
          in: path
          required: true
          schema:
            type: integer
          description: ID postingan borrow milik user saat ini
        - name: requestId
          in: path
          required: true
          schema:
            type: integer
          description: ID request peminjaman yang ingin diambil detailnya
        - name: Authorization
          in: header
          required: true
          schema:
            type: string
          description: JWT token (Bearer)
        - name: Accept-Language
          in: header
          required: false
          schema:
            type: string
          description: Bahasa response (en/id)
      responses:
        '200':
          description: Detail incoming borrow request berhasil diambil
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: true
                  data:
                    type: object
                    properties:
                      id:
                        type: integer
                        example: 10
                      item_name:
                        type: string
                        example: "Kamera DSLR Canon"
                      description:
                        type: string
                        example: "Kamera masih mulus, lengkap dengan lensa kit."
                      images:
                        type: array
                        items:
                          type: string
                        example: ["https://cdn.cyclefi.id/borrow/10-1.jpg"]
                      category:
                        type: object
                        properties:
                          id:
                            type: integer
                            example: 2
                          name:
                            type: string
                            example: "Elektronik"
                      status:
                        type: object
                        properties:
                          id:
                            type: integer
                            example: 1
                          status:
                            type: string
                            example: "waiting_for_request"
                          updated_at:
                            type: string
                            format: date-time
                            example: "2025-09-03T10:00:00.000Z"
                      user:
                        type: object
                        properties:
                          id:
                            type: integer
                            example: 1
                          profile_picture:
                            type: string
                            example: "https://cdn.cyclefi.id/user/1.jpg"
                          username:
                            type: string
                            example: "andi123"
                          fullname:
                            type: string
                            example: "Andi"
                      address:
                        type: object
                        properties:
                          id:
                            type: integer
                            example: 3
                          address:
                            type: string
                            example: "Jl. Merdeka No. 1, Bandung"
                          latitude:
                            type: number
                            example: -6.914744
                          longitude:
                            type: number
                            example: 107.609810
                      phone:
                        type: string
                        example: "08123456789"
                      duration_from:
                        type: string
                        format: date
                        example: "2025-09-05"
                      duration_to:
                        type: string
                        format: date
                        example: "2025-09-12"
                      borrowing_duration:
                        type: integer
                        example: 7
                      requested_by:
                        type: object
                        properties:
                          id:
                            type: integer
                            example: 21
                          reason:
                            type: string
                            example: "Untuk tugas kuliah"
                          address:
                            type: object
                            properties:
                              id:
                                type: integer
                                example: 5
                              address:
                                type: string
                                example: "Jl. Asia Afrika No. 2, Bandung"
                              latitude:
                                type: number
                                example: -6.921
                              longitude:
                                type: number
                                example: 107.607
                          distance:
                            type: number
                            example: 2.5
                          user:
                            type: object
                            properties:
                              id:
                                type: integer
                                example: 7
                              profile_picture:
                                type: string
                                example: "https://cdn.cyclefi.id/user/7.jpg"
                              fullname:
                                type: string
                                example: "Budi"
                              username:
                                type: string
                                example: "budi123"
                          status:
                            type: object
                            properties:
                              id:
                                type: integer
                                example: 1
                              status:
                                type: string
                                example: "pending"
                              updated_at:
                                type: string
                                format: date-time
                                example: "2025-09-03T10:00:00.000Z"
                          phone:
                            type: string
                            example: "08123456789"
                          duration_from:
                            type: string
                            format: date
                            example: "2025-09-06"
                          duration_to:
                            type: string
                            format: date
                            example: "2025-09-10"
                          borrowing_duration:
                            type: integer
                            example: 4
        '404':
          description: Borrow post atau request tidak ditemukan atau bukan milik user
        '401':
          description: Unauthorized

  /api/users/current/borrows/{borrowId}/requests/{requestId}/process:
    post:
      tags:
        - Borrow
      summary: Process incoming borrow request (accept/decline)
      description: |
        Memproses incoming request ke postingan borrow milik user saat ini. Bisa accept (menerima) atau decline (menolak) request. Jika accept, request lain otomatis cancelled.
      parameters:
        - name: borrowId
          in: path
          required: true
          schema:
            type: integer
          description: ID postingan borrow milik user saat ini
        - name: requestId
          in: path
          required: true
          schema:
            type: integer
          description: ID request peminjaman yang ingin diproses
        - name: Authorization
          in: header
          required: true
          schema:
            type: string
          description: JWT token (Bearer)
        - name: Accept-Language
          in: header
          required: false
          schema:
            type: string
          description: Bahasa response (en/id)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                action:
                  type: string
                  enum: [accept, decline]
                  description: Aksi yang ingin dilakukan (accept/decline)
                  example: accept
                decline_reason:
                  type: string
                  description: Alasan penolakan **(wajib jika action=decline)**
                  example: "Maaf, barang sedang tidak tersedia"
      responses:
        '200':
          description: Request berhasil diproses
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: Pengajuan peminjaman berhasil dikonfirmasi      
        '400':
          description: Validasi gagal atau status tidak sesuai
        '404':
          description: Borrow post atau request tidak ditemukan atau bukan milik user
        '401':
          description: Unauthorized

  /api/users/current/borrows/{borrowId}/requests/{requestId}/extend:
    post:
      tags:
        - Borrow
      summary: Perpanjang durasi peminjaman dari request yang di accept
      description: |
        Memperpanjang (extend) duration_to pada request peminjaman (borrow request) yang sudah di-accept (dengan status Borrowed/Overdue) pada postingan borrow milik user saat ini.
        - Hanya dapat dilakukan oleh pemilik borrowId.
        - Status terakhir borrowId harus Lent atau Overdue.
        - Status terakhir requestId harus Borrowed atau Overdue.
        - duration_to baru pada requestId harus lebih dari duration_to sebelumnya dan tidak boleh melebihi duration_to pada borrowId.
        - Setelah extend, status history pada borrow dan borrowApplication akan ditambah dengan status Extended.
      parameters:
        - name: borrowId
          in: path
          required: true
          schema:
            type: integer
          description: ID postingan borrow milik user saat ini
        - name: requestId
          in: path
          required: true
          schema:
            type: integer
          description: ID request peminjaman yang ingin di-extend
        - name: Authorization
          in: header
          required: true
          schema:
            type: string
          description: JWT token (Bearer)
        - name: Accept-Language
          in: header
          required: false
          schema:
            type: string
          description: Bahasa response (en/id)
        - name: Content-Type
          in: header
          required: true
          schema:
            type: string
            example: application/json
          description: Tipe konten yang dikirim dalam format application/json
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                duration_to:
                  type: string
                  format: date
                  example: "2025-09-15"
                  description: Tanggal baru pengembalian (harus lebih dari duration_to sebelumnya dan <= duration_to milik borrowId)
              required:
                - duration_to
      responses:
        "201":
          description: Extend borrow request berhasil
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: "Borrow request extended successfully"
                  data:
                    type: object
                    properties:
                      id:
                        type: integer
                        example: 12
                      borrow_id:
                        type: integer
                        example: 10
                      duration_from:
                        type: string
                        format: date
                        example: "2025-09-05"
                      duration_to:
                        type: string
                        format: date
                        example: "2025-09-15"
                      status:
                        type: string
                        example: "Extended"
        "400":
          description: "Validasi gagal atau status tidak sesuai (misal: duration_to tidak valid, status tidak sesuai)"
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  message:
                    type: string
                    example: "Validasi gagal"
                  errors:
                    type: object
                    example:
                      duration_to: "Tanggal extend tidak valid"
        "401":
          description: Unauthorized (token tidak valid/expired)
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  message:
                    type: string
                    example: "Unauthorized. Need Authorization Token"
                  errors:
                    type: object
                    properties:
                      token:
                        type: string
                        example: null
        "404":
          description: Data borrow atau borrow request tidak ditemukan
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  message:
                    type: string
                    example: "Data tidak ditemukan"
                  errors:
                    type: object
                    example: null

  /api/recycle-locations:
    get:
      tags:
        - Recycle
      summary: Mendapatkan daftar recycle locations (tempat daur ulang)
      description: |
        Mengambil daftar recycle locations (tempat daur ulang) beserta kategori, gambar, dan jarak terdekat dari semua alamat user login.
        Mendukung pencarian nama tempat lokasi, filter kategori, filter lokasi (maxDistance atau nama lokasi), dan sorting.
      parameters:
        - name: Authorization
          in: header
          required: true
          schema:
            type: string
          description: JWT token (Bearer)
        - name: Accept-Language
          in: header
          required: false
          schema:
            type: string
          description: Bahasa response (en/id)
        - name: search
          in: query
          required: false
          schema:
            type: string
          description: Pencarian pada location_name (case-insensitive, partial match)
        - name: category
          in: query
          required: false
          schema:
            type: string
            example: "furniture,clothes"
          description: Filter berdasarkan nama kategori (bisa multiple, pisahkan dengan koma)
        - name: maxDistance
          in: query
          required: false
          schema:
            type: number
            example: 5
          description: Filter lokasi dengan jarak maksimum (km) dari address user login. Jika diisi, parameter location diabaikan.
        - name: location
          in: query
          required: false
          schema:
            type: string
            example: "Bandung"
          description: Filter berdasarkan nama lokasi (address). Diabaikan jika maxDistance diisi.
        - name: sortBy
          in: query
          required: false
          schema:
            type: string
            enum: [relevance, nearest]
          description: Urutan hasil (relevance, nearest). Default relevance
        - name: page
          in: query
          required: false
          schema:
            type: integer
            default: 1
          description: "Halaman (default: 1)"
        - name: size
          in: query
          required: false
          schema:
            type: integer
            default: 10
          description: "Jumlah data per halaman (default: 10)"
      responses:
        "200":
          description: Daftar recycle locations berhasil diambil
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: Berhasil mendapatkan daftar recycle locations
                  meta:
                    type: object
                    properties:
                      total:
                        type: integer
                        example: 100
                      page:
                        type: integer
                        example: 1
                      size:
                        type: integer
                        example: 10
                      totalPages:
                        type: integer
                        example: 10
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: integer
                          example: 1
                        location_name:
                          type: string
                          example: "Recycle Center Bandung"
                        address:
                          type: string
                          example: "Jl. Merdeka No. 1, Bandung"
                        latitude:
                          type: string
                          example: "-6.914744"
                        longitude:
                          type: string
                          example: "107.609810"
                        distance:
                          type: string
                          example: "2.5"
                          description: "Jarak terdekat (km) dari daftar alamat user login"
                        categories:
                          type: array
                          items:
                            type: object
                            properties:
                              id:
                                type: int
                                example: 1
                              name:
                                type: string
                                example: clothes
                        images:
                          type: array
                          items:
                            type: string
                          example:
                            - "https://dummyimage.com/600x400/000/fff&text=recycle-location1-1"
                            - "https://dummyimage.com/600x400/000/fff&text=recycle-location1-2"
        "401":
          description: Unauthorized (token tidak valid/expired)
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  message:
                    type: string
                    example: "Unauthorized. Need Authorization Token"
                  errors:
                    type: object
                    properties:
                      token:
                        type: string
                        example: null
        "404":
          description: User tidak punya alamat
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  message:
                    type: string
                    example: User does not have an address yet

  /api/recycle-locations/{recyleLocationId}:
    get:
      tags:
        - Recycle
      summary: Mendapatkan detail recycle locations (tempat daur ulang)
      description: |
        Mengambil detail recycle locations (tempat daur ulang) beserta kategori, gambar, dan jarak terdekat dari semua alamat user login
      parameters:
        - name: Authorization
          in: header
          required: true
          schema:
            type: string
          description: JWT token (Bearer)
        - name: Accept-Language
          in: header
          required: false
          schema:
            type: string
          description: Bahasa response (en/id)
        - name: recycleLocationId
          in: path
          required: true
          schema:
            type: string
          description: ID Recycle Location yang ingin diambil detailnya
      responses:
        "200":
          description: Daftar recycle locations berhasil diambil
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: Berhasil mendapatkan detail tempat daur ulang
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: integer
                          example: 1
                        location_name:
                          type: string
                          example: "Recycle Center Bandung"
                        address:
                          type: string
                          example: "Jl. Merdeka No. 1, Bandung"
                        latitude:
                          type: string
                          example: "-6.914744"
                        longitude:
                          type: string
                          example: "107.609810"
                        distance:
                          type: string
                          example: "2.5"
                          description: "Jarak terdekat (km) dari daftar alamat user login"
                        phone:
                          type: string
                          example: "+6213737865862"
                        description:
                          type: string
                          example: "Absque sopor comedo. Id pariatur aggero. Accusator surgo modi ullam ulciscor. Aliqua itaque conforto despecto. Curatio auctor vulgus acer admoveo compello stillicidium spectaculum. Cui beatus viscus cribro timidus. Expedita aeger tergo confugo timidus ipsam desino. Decens desolo volaticus paulatim calco volubilis aequitas."
                        categories:
                          type: array
                          items:
                            type: object
                            properties:
                              id:
                                type: int
                                example: 1
                              name:
                                type: string
                                example: clothes
                        images:
                          type: array
                          items:
                            type: string
                          example:
                            - "https://dummyimage.com/600x400/000/fff&text=recycle-location1-1"
                            - "https://dummyimage.com/600x400/000/fff&text=recycle-location1-2"
        "401":
          description: Unauthorized (token tidak valid/expired)
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  message:
                    type: string
                    example: "Unauthorized. Need Authorization Token"
                  errors:
                    type: object
                    properties:
                      token:
                        type: string
                        example: null
        "404":
          description: User tidak punya alamat
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  message:
                    type: string
                    example: User does not have an address yet

  /api/recycles:
    post:
      tags:
        - Recycle
      summary: Membuat recycle baru
      description: |
        Endpoint untuk membuat postingan recycle baru. Hanya user yang sudah login yang dapat mengakses endpoint ini.
      parameters:
        - name: Authorization
          in: header
          required: true
          schema:
            type: string
          description: JWT token (Bearer)
        - name: Accept-Language
          in: header
          required: false
          schema:
            type: string
          description: Bahasa response (en/id)
        - name: Content-Type
          in: header
          required: true
          schema:
            type: string
            example: multipart/form-data
          description: Tipe konten yang dikirim dalam format multipart/form-data karena ada file yang disertakan
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                item_name:
                  type: string
                  maxLength: 255
                  example: Sepeda Bekas
                  description: Nama barang yang didonasikan
                description:
                  type: string
                  maxLength: 255
                  example: Sepeda bekas, masih layak pakai, warna biru.
                  description: Deskripsi barang donasi
                category_id:
                  type: integer
                  example: 1
                  description: ID kategori donasi
                address_id:
                  type: integer
                  example: 2
                  description: ID alamat pengambilan barang
                phone_id:
                  type: integer
                  example: 3
                  description: ID nomor kontak yang bisa dihubungi
                images:
                  type: array
                  items:
                    type: string
                    format: binary
                  # description: Daftar file gambar donasi (maksimal 5 gambar)
                recycle_location_id:
                  type: integer
                  example: 1
                  description: ID recycle location
              required:
                - item_name
                - description
                - category_id
                - address_id
                - phone_id
                - images
                - recycle_location_id
      responses:
        '201':
          description: Donasi berhasil dibuat
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: Donasi berhasil dibuat
                  data:
                    type: object
                    properties:
                      id:
                        type: integer
                        example: 10
                      item_name:
                        type: string
                        example: Sepeda Bekas
                      description:
                        type: string
                        example: Sepeda bekas, masih layak pakai, warna biru.
                      category:
                        type: object
                        properties:
                          id:
                            type: integer
                            example: 1
                          name:
                            type: string
                            example: Sepeda
                      address_id:
                        type: integer
                        example: 2
                      phone_id:
                        type: integer
                        example: 3
                      image:
                        type: array
                        items:
                          type: string
                        example:
                          - https://.../donation_1_1692950400000.jpg
                          - https://.../donation_1_1692950401000.jpg
                      status:
                        type: string
                        example: submitted
                      location:
                        type: object
                        properties:
                          id:
                            type: int
                            example: 1
                          name:
                            type: string
                            example: Luettgen and Sons Recycle Center
                          address:
                            type: string
                            example: Gang Babakan Jati Mulya IV, Gumuruh, Batununggal, Kota Bandung, Jawa Barat, Jawa, 40275, Indonesia
                          latitude:
                            type: int 
                            example: -6.9229148
                          longitude:
                            type: int 
                            example: 107.7026636
        '400':
          description: Validasi gagal atau request tidak sesuai
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  message:
                    type: string
                    example: Validasi gagal
                  errors:
                    type: object
                    example:
                      item_name:
                        - Field item_name wajib diisi
        "401":
          description: Unauthorized (token tidak valid/expired)
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  message:
                    type: string
                    example: "Unauthorized. Need Authorization Token"
                  errors:
                    type: object
                    properties:
                      token:
                        type: string
                        example: null

  /api/users/current/recycles:
    get:
      tags:
        - Recycle
      summary: Mendapatkan riwayat recycle user
      description: |
        Mengambil daftar recycle milik user saat ini dengan filter kategori, status, search, dan pagination.
      parameters:
        - name: Authorization
          in: header
          required: true
          schema:
            type: string
          description: JWT token (Bearer)
        - name: Accept-Language
          in: header
          required: false
          schema:
            type: string
          description: Bahasa response (en/id)
        - name: Content-Type
          in: header
          required: true
          schema:
            type: string
            example: application/json
          description: Tipe konten yang dikirim dalam format application/json
        - name: search
          in: query
          description: Pencarian nama item atau deskripsi (optional)
          required: false
          schema:
            type: string
        - name: category
          in: query
          description: Filter berdasarkan nama kategori (bisa multiple, pisahkan dengan koma, gunakan "all" untuk semua)
          required: false
          schema:
            type: string
            example: elektronik,perabot
        - name: status
          in: query
          description: "Filter status recycle **(value: submitted, confirmed, confirmed, completed, failed) atau all**"
          required: false
          schema:
            type: string
            example: submitted,confirmed
        - name: page
          in: query
          description: "Halaman data recycle milik user (default: 1)"
          required: false
          schema:
            type: integer
            default: 1
        - name: size
          in: query
          description: "Jumlah data per halaman recycle milik user (default: 10)"
          required: false
          schema:
            type: integer
            default: 10
      responses:
        '200':
          description: Berhasil mendapatkan riwayat recycle user
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: Berhasil mendapatkan riwayat recycle
                  meta:
                    type: object
                    properties:
                      total:
                        type: integer
                        example: 12
                      page:
                        type: integer
                        example: 1
                      size:
                        type: integer
                        example: 10
                      totalPages:
                        type: integer
                        example: 2
                  data:
                    type: array
                    items:
                        type: object
                        properties:
                          id:
                            type: integer
                          item_name:
                            type: string
                          description:
                            type: string
                          images:
                            type: array
                            items:
                              type: string
                              format: uri
                          category:
                            type: object
                            nullable: true
                            properties:
                              id:
                                type: integer
                              name:
                                type: string
                          status:
                            type: object
                            nullable: true
                            properties:
                              id:
                                type: integer
                              status:
                                type: string
                              updated_at:
                                type: string
                                format: date-time
        "401":
          description: Unauthorized (token tidak valid/expired)
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  message:
                    type: string
                    example: "Unauthorized. Need Authorization Token"
                  errors:
                    type: object
                    properties:
                      token:
                        type: string
                        example: null

  /api/users/current/recycles/{recycleId}:
    get:
      tags:
        - Recycle
      summary: Mendapatkan detail postingan recycle milik user sendiri
      description: |
        Mengambil detail postingan recycle milik user saat ini berdasarkan recycleId
      parameters:
        - name: recycleId
          in: path
          required: true
          description: ID recycle milik user login
          schema:
            type: integer
        - name: Authorization
          in: header
          required: true
          schema:
            type: string
          description: JWT token (Bearer)
        - name: Accept-Language
          in: header
          required: false
          schema:
            type: string
          description: Bahasa response (en/id)
        - name: Content-Type
          in: header
          required: true
          schema:
            type: string
            example: application/json
          description: Tipe konten yang dikirim dalam format application/json
      responses:
        '200':
          description: Berhasil mendapatkan detail recycle milik user sendiri
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: Berhasil mendapatkan detail daur ulang
                  data:
                    type: object
                    properties:
                      id:
                        type: integer
                        example: 10
                      item_name:
                        type: string
                        example: "Sepeda Bekas"
                      description:
                        type: string
                        example: "Sepeda bekas, masih layak pakai, warna biru."
                      category:
                        type: object
                        properties:
                          id:
                            type: integer
                            example: 1
                          name:
                            type: string
                            example: "Sepeda"
                      status:
                        type: string
                        example: "waiting_for_confirmation"
                      address:
                        type: object
                        properties:
                          id:
                            type: integer
                            example: 2
                          name:
                            type: string
                            example: Rumah
                          address:
                            type: string
                            example: "Jl. Mawar No. 1 Bandung, Jawa Barat, Indonesia"
                          latitude:
                            type: number
                            example: -6.123
                          longitude:
                            type: number
                            example: 106.123
                      images:
                        type: array
                        items:
                          type: string
                          format: uri
                        example:
                          - "https://..."
                          - "https://..."
                      status_histories:
                        type: array
                        items:
                          type: object
                          properties:
                            id:
                              type: integer
                              example: 1
                            status:
                              type: string
                              example: "waiting_for_request"
                            status_detail:
                              type: string
                              example: "Menunggu permintaan barter"
                            updated_at:
                              type: string
                              format: date-time
                              example: "2025-08-26T10:00:00.000Z"
        '400':
          description: recycleId tidak valid
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  message:
                    type: string
                    example: ID Recycle bukan angka
                  errors:
                    type: object
                    properties:
                      token:
                        type: string
                        example: null
        "401":
          description: Unauthorized (token tidak valid/expired)
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  message:
                    type: string
                    example: "Unauthorized. Need Authorization Token"
                  errors:
                    type: object
                    properties:
                      token:
                        type: string
                        example: null
        '404':
          description: Recycle tidak ditemukan
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  message:
                    type: string
                    example: Postingan daur ulang tidak ditemukan
                  errors:
                    type: object
                    properties:
                      token:
                        type: string
                        example: null

  /api/categories/{categoryId}/repair-prices:
    get:
      tags:
        - Repair
      summary: Mendapatkan harga perbaikan pada sebuah kategori
      description: |
        Mengambil data harga perbaikan tipe minor, moderate, major pada sebuah kategori
      parameters:
        - name: categoryId
          in: path
          description: Category yang ingin diambil data harga perbaikannya
          schema:
            type: string
          required: true
        - name: Authorization
          in: header
          required: true
          schema:
            type: string
          description: JWT token (Bearer)
        - name: Accept-Language
          in: header
          required: false
          schema:
            type: string
          description: Bahasa response (en/id)
      responses:
        '202':
          description: Berhasil mendapatkan harga perbaikan
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean,
                    example: true
                  message:
                    type: string
                    example: Get repair price list of a categories succesful
                  data:
                    type: object
                    properties:
                      id:
                        type: int
                        example: 1
                      category:
                        type: object
                        properties:
                          id:
                            type: int
                            example: 1
                          name:
                            type: string
                            example: Books
                      minor_price:
                        type: int
                        example: 10016
                      moderate_price:
                        type: int
                        example: 28458
                      major_price:
                        type: int
                        example: 33925
        '404':
          description: Data category tidak ditemukan

  /api/repairs:
    post:
      tags:
        - Repair
      summary: Membuat permintaan perbaikan barang (repair)
      description: |
        Endpoint untuk membuat permintaan perbaikan barang. User mengisi data item, kategori, berat, tipe perbaikan, alamat, kontak, lokasi perbaikan, dan upload gambar (front_view & close_up_damage).
        - Gambar wajib diupload minimal 1 untuk masing-masing tipe.
        - Harga dihitung otomatis: repair_price * item_weight.
        - Status awal: request_submitted, payment status: pending.
      parameters:
        - name: Authorization
          in: header
          required: true
          schema:
            type: string
          description: JWT token (Bearer)
        - name: Accept-Language
          in: header
          required: false
          schema:
            type: string
          description: Bahasa response (en/id)
        - name: Content-Type
          in: header
          required: true
          schema:
            type: string
            example: multipart/form-data
          description: Tipe konten multipart/form-data (karena upload file)
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                item_name:
                  type: string
                  maxLength: 255
                  example: Kipas Angin Rusak
                  description: Nama barang yang ingin diperbaiki
                description:
                  type: string
                  maxLength: 255
                  example: Kipas angin mati total, ingin diperbaiki.
                  description: Deskripsi kerusakan barang
                category_id:
                  type: integer
                  example: 1
                  description: ID kategori barang
                item_weight:
                  type: number
                  example: 2.5
                  description: Berat barang (kg)
                repair_type:
                  type: string
                  enum: [minor_repair, moderate_repair, major_repair]
                  example: moderate_repair
                  description: Tipe perbaikan
                address_id:
                  type: integer
                  example: 3
                  description: ID alamat user
                phone_id:
                  type: integer
                  example: 4
                  description: ID nomor kontak user
                repair_location:
                  type: string
                  enum: [my_location, warehouse]
                  example: my_location
                  description: Lokasi perbaikan
                front_view:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: Gambar tampak depan barang (wajib, max 5)
                close_up_damage:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: Gambar close up kerusakan (wajib, max 5)
              required:
                - item_name
                - description
                - category_id
                - item_weight
                - repair_type
                - address_id
                - phone_id
                - repair_location
                - front_view
                - close_up_damage
      responses:
        "201":
          description: Repair berhasil dibuat
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: Repair created successfully
                  data:
                    type: object
                    properties:
                      id:
                        type: integer
                        example: 12
                      item_name:
                        type: string
                        example: Kipas Angin Rusak
                      description:
                        type: string
                        example: Kipas angin mati total, ingin diperbaiki.
                      category:
                        type: object
                        properties:
                          id:
                            type: integer
                            example: 1
                          name:
                            type: string
                            example: Electronics
                      item_weight:
                        type: number
                        example: 2.5
                      repair_type:
                        type: string
                        example: moderate_repair
                      address_id:
                        type: object
                        properties:
                          id:
                            type: int
                            example: 1
                          address:
                            type: string
                      phone_id:
                        type: object
                        properties:
                          id:
                            type: int
                            example: 1
                          number:
                            type: string
                            example: "+6281122334455"
                      repair_location:
                        type: string
                        example: my_location
                      images:
                        type: array
                        items:
                          type: string
                          format: uri
                        example:
                          - https://api.cyclefi.id/assets/repairs/12_5_front_view_1693850000000.jpg
                          - https://api.cyclefi.id/assets/repairs/12_5_close_up_damage_1693850001000.jpg
                      status:
                        type: string
                        example: Request Submitted
                      payment:
                        type: object
                        properties:
                          id:
                            type: integer
                            example: 20
                          repair_id:
                            type: integer
                            example: 12
                          order_id:
                            type: string
                            example: REPAIR-1-16-1012715231
                          user:
                            type: object
                            properties:
                              id:
                                type: integer
                                example: 5
                              profile_picture:
                                type: string
                                example: https://api.cyclefi.id/assets/profiles/username.png
                              username:
                                type: string
                                example: budi123
                              fullname:
                                type: string
                                example: Budi Santoso
                          amount:
                            type: integer
                            example: 31250
                            description: Repair cost
                          admin_fee:
                            type: integer
                            example: 1563
                            description: 5% repair cost (Pembulatan keatas)
                          total:
                            type: integer
                            example: 32813
                            description: amount + admin fee
                          status:
                            type: string
                            example: Pending
        "400":
          description: Validasi gagal atau request tidak sesuai
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  message:
                    type: string
                    example: Validasi gagal
                  errors:
                    type: object
                    example:
                      item_name:
                        - Kolom item_name wajib diisi
        "401":
          description: Unauthorized (token tidak valid/expired)
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  message:
                    type: string
                    example: Unauthorized. Need Authorization Token
                  errors:
                    type: object
                    properties:
                      token:
                        type: string
                        example: null
        "404":
          description: Data kategori/alamat/phone tidak ditemukan atau tidak dimiliki user
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  message:
                    type: string
                    example: Data tidak ditemukan

  /api/repairs/{repairId}:
    get:
      tags:
        - Repair
      summary: Mendapatkan detail yang sama persis seperti di `POST /api/repairs` diatas
      description: Mendapatkan detail repair yang telah dibuat di POST /api/repairs dengan **struktur response yang sama**

  /api/repairs/{repairId}/pay:
    post:
      tags:
        - Repair
      summary: Request payment for a repair
      description: |
        Membuat permintaan pembayaran untuk repair tertentu. 
        Jika pembayaran sudah ada dan masih pending, instruksi pembayaran yang sama akan dikembalikan.
        Jika pembayaran sudah expired/failed/cancelled, akan dibuat pembayaran baru (order_id baru).
        Mendukung metode pembayaran: bank_transfer (VA), qris, e_wallet (gopay, shopeepay, dll).
      parameters:
        - name: repairId
          in: path
          required: true
          description: ID repair yang akan dibayar
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - paymentType
              properties:
                paymentType:
                  type: string
                  enum: [bank_transfer, qris, e_wallet]
                  description: Jenis pembayaran yang dipilih user (bank_transfer, qris, e_wallet)
                bankCode:
                  type: string
                  description: "Kode bank (wajib jika paymentType=bank_transfer). Value yang tersedia: **bca, bni, bri, bsi, cimb**"
                ewalletType:
                  type: string
                  description: "Jenis e-wallet (wajib jika paymentType=e_wallet). Value yang tersedia: **gopay**"
      responses:
        '200':
          description: Payment instruction created or returned successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      repair_id:
                        type: integer
                        example: 1
                      item_name:
                        type: string
                        example: Electric Kettle
                      category:
                        type: object
                        properties:
                          id:
                            type: integer
                            example: 1
                          name:
                            type: string
                            example: Furniture
                      customer:
                        type: string
                        example: John Doe
                      order_id:
                        type: string
                        example: "REPAIR-1-1-1756988995278"
                      amount:
                        type: integer
                        example: 31250
                        description: Repair cost
                      admin_fee:
                        type: integer
                        example: 1563
                        description: 5% repair cost (Pembulatan keatas)
                      total:
                        type: integer
                        example: 32813
                        description: amount + admin fee
                      payment_type:
                        type: string
                        example: qris
                      status:
                        type: string
                        example: pending
                      expiry:
                        type: string
                        format: date-time
                      # Bank Transfer
                      bank_code:
                        type: string
                        nullable: true
                      va_number:
                        type: string
                        nullable: true
                      # QRIS
                      qris_url:
                        type: string
                        nullable: true
                      # E-Wallet
                      ewallet_type:
                        type: string
                        nullable: true
                      deeplink_url:
                        type: string
                        nullable: true
        '400':
          description: Invalid request or payment type
        '403':
          description: Anda tidak memiliki akses ke resource repair payment tersebut
        '404':
          description: Resource tidak ditemukan

  /api/repairs/{repairId}/payment-status:
    get:
      summary: Get payment status for a repair
      tags:
        - Repair
      parameters:
        - name: repairId
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Payment status detail
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      id:
                        type: integer
                      order_id:
                        type: string
                      amount:
                        type: integer
                      admin_fee:
                        type: integer
                      total:
                        type: integer
                      customer:
                        type: string
                      item_name:
                        type: string
                      category:
                        type: object
                        properties:
                          id:
                            type: integer
                          name:
                            type: string
                      expired_at:
                        type: string
                        format: date-time
                        nullable: true
                      status:
                        type: string
                        enum: [pending, paid, expired, cancelled, failed]
                      payment_type:
                        type: string
                        enum: [bank_transfer, e_wallet, qris]
                        nullable: true
                      bank_code:
                        type: string
                        nullable: true
                      va_number:
                        type: string
                        nullable: true
                      ewallet_type:
                        type: string
                        nullable: true
                      deeplink_url:
                        type: string
                        nullable: true
                      qris_url:
                        type: string
                        nullable: true
                      paid_at:
                        type: string
                        format: date-time
                        nullable: true
        '401':
          description: Unauthorized
        '404':
          description: Repair/payment not found

  /api/users/current/repairs:
    get:
      tags:
        - Repair
      summary: Mendapatkan riwayat recycle user
      description: |
        Mengambil daftar perbaikan milik user saat ini dengan filter kategori, status, search, dan pagination.
      parameters:
        - name: Authorization
          in: header
          required: true
          schema:
            type: string
          description: JWT token (Bearer)
        - name: Accept-Language
          in: header
          required: false
          schema:
            type: string
          description: Bahasa response (en/id)
        - name: Content-Type
          in: header
          required: true
          schema:
            type: string
            example: application/json
          description: Tipe konten yang dikirim dalam format application/json
        - name: search
          in: query
          description: Pencarian nama item atau deskripsi (optional)
          required: false
          schema:
            type: string
        - name: category
          in: query
          description: Filter berdasarkan nama kategori (bisa multiple, pisahkan dengan koma, gunakan "all" untuk semua)
          required: false
          schema:
            type: string
            example: elektronik,perabot
        - name: status
          in: query
          description: "Filter status recycle **(value: request_submitted, confirmed, under_repair, completed, failed) atau all**"
          required: false
          schema:
            type: string
            example: request_submitted,confirmed
        - name: repairLocation
          in: query
          description: "Filter lokasi perbaikan **(value: my_location, warehouse) atau all**"
          required: false
          schema:
            type: string
            example: my_location
        - name: repairType
          in: query
          description: "Filter jenis perbaikan **(value: minor_repair, moderate_repair, major_repair) atau all **"
          required: false
          schema:
            type: string
            example: all
        - name: page
          in: query
          description: "Halaman data recycle milik user (default: 1)"
          required: false
          schema:
            type: integer
            default: 1
        - name: size
          in: query
          description: "Jumlah data per halaman recycle milik user (default: 10)"
          required: false
          schema:
            type: integer
            default: 10
      responses:
        '200':
          description: Berhasil mendapatkan riwayat repair user
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: Berhasil mendapatkan riwayat perbaikan
                  meta:
                    type: object
                    properties:
                      total:
                        type: integer
                        example: 12
                      page:
                        type: integer
                        example: 1
                      size:
                        type: integer
                        example: 10
                      totalPages:
                        type: integer
                        example: 2
                  data:
                    type: array
                    items:
                        type: object
                        properties:
                          id:
                            type: integer
                          item_name:
                            type: string
                          description:
                            type: string
                          images:
                            type: array
                            items:
                              type: object
                              properties:
                                id:
                                  type: int
                                  example: 1
                                image_path:
                                  type: string
                                  format: uri
                                image_size:
                                  type: integer
                                  example: 2501
                                image_name:
                                  type: string
                                  example: front-view1.png
                                image_type:
                                  type: string
                                  example: Front View
                          category:
                            type: object
                            nullable: true
                            properties:
                              id:
                                type: integer
                              name:
                                type: string
                          repair_location:
                            type: string
                            example: Warehouse
                          repair_type:
                            type: string
                            example: Moderate Repair
                          status:
                            type: object
                            nullable: true
                            properties:
                              id:
                                type: integer
                              status:
                                type: string
                              updated_at:
                                type: string
                                format: date-time
        "401":
          description: Unauthorized (token tidak valid/expired)
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  message:
                    type: string
                    example: "Unauthorized. Need Authorization Token"
                  errors:
                    type: object
                    properties:
                      token:
                        type: string
                        example: null

  /api/users/current/repairs/{repairId}:
    get:
      tags:
        - Repair
      summary: Mendapatkan detail postingan repair milik user sendiri
      description: |
        Mengambil detail postingan repair milik user saat ini berdasarkan repairId
      parameters:
        - name: repairId
          in: path
          required: true
          description: ID repair milik user login
          schema:
            type: integer
        - name: Authorization
          in: header
          required: true
          schema:
            type: string
          description: JWT token (Bearer)
        - name: Accept-Language
          in: header
          required: false
          schema:
            type: string
          description: Bahasa response (en/id)
        - name: Content-Type
          in: header
          required: true
          schema:
            type: string
            example: application/json
          description: Tipe konten yang dikirim dalam format application/json
      responses:
        '200':
          description: Berhasil mendapatkan detail riwayat repair
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: Berhasil mendapatkan detail riwayat perbaikan
                  data:
                    type: object
                    properties:
                      id:
                        type: integer
                        example: 10
                      item_name:
                        type: string
                        example: "Sepeda Bekas"
                      description:
                        type: string
                        example: "Sepeda bekas, masih layak pakai, warna biru."
                      category:
                        type: object
                        properties:
                          id:
                            type: integer
                            example: 1
                          name:
                            type: string
                            example: "Sepeda"
                      repair_location:
                        type: string
                        example: Warehouse
                      repair_type:
                        type: string
                        example: Moderate Repair
                      status:
                        type: string
                        example: "Request Submitted"
                      address:
                        type: object
                        properties:
                          id:
                            type: integer
                            example: 2
                          name:
                            type: string
                            example: Rumah
                          address:
                            type: string
                            example: "Jl. Mawar No. 1 Bandung, Jawa Barat, Indonesia"
                          latitude:
                            type: number
                            example: -6.123
                          longitude:
                            type: number
                            example: 106.123
                      images:
                        type: array
                        items:
                          type: object
                          properties:
                            id:
                              type: int
                              example: 1
                            image_path:
                              type: string
                              format: uri
                            image_size:
                              type: integer
                              example: 2501
                            image_name:
                              type: string
                              example: front-view1.png
                            image_type:
                              type: string
                              example: Front View
                      status_histories:
                        type: array
                        items:
                          type: object
                          properties:
                            id:
                              type: integer
                              example: 1
                            status:
                              type: string
                              example: "waiting_for_request"
                            status_detail:
                              type: string
                              example: "Menunggu permintaan barter"
                            updated_at:
                              type: string
                              format: date-time
                              example: "2025-08-26T10:00:00.000Z"
        '400':
          description: repairId tidak valid
        "401":
          description: Unauthorized (token tidak valid/expired)
        '404':
          description: Repair tidak ditemukan



















